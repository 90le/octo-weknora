# 项目进度

更新：2026-09-23。范围见 [需求地图](OCTO-REQUIREMENTS.md)，接续顺序见 [实施计划](OCTO-PLAN.md)，配置见 [OCTO-SETUP](OCTO-SETUP.md)，操作布局见 [OCTO-UX](OCTO-UX.md)。本页是唯一的当前进度入口；下方旧日期段落是历史发布证据。

## 当前状态（2026-09-23 15:58 CST）

**隐私修复已发布。**[PR #41](https://github.com/90le/octo-weknora/pull/41)、[#42](https://github.com/90le/octo-weknora/pull/42)、[#43](https://github.com/90le/octo-weknora/pull/43)、[#45](https://github.com/90le/octo-weknora/pull/45) 已合并；最后的 #45 修复 QA 错误路径与 GORM SQL 警告泄露。生产 app 的不可变 Image ID 已回读为 `sha256:64e8489264404dc9d507efa544edbf4db9753d502775ca17d275be2ab90601bd`，对应合并提交 `43030dd1c2cdb2b75c82d28380c0c3d109369eea`，构建源归档校验值前缀 `7723ce70`；UI 仍为 `sha256:1406a7f1…`。四项 PR 的 Go 测试／构建与 lint 检查通过，#45 的 scope 检查也通过。生产后端健康 200、未认证知识 API 401、前端 200；认证系统信息返回完整 `43030dd1` 与 migration `105`、4 个知识库、Anydoc 可用。PostgreSQL migration `105` 非 dirty，4 个活动知识库、60 条活动来源、4 条问题，运行中的同步／解析均为 0；IM 通道启用 1 条、inbox 处理中 0 条，Octo 小丘 WebSocket 运行时已初始化。私人 OpenClaw Gateway 与 Octo daemon 保持 active。

**真实群验收与隐私回读：**授权的「Octo 小测」在「Steward 联调群」原生 @ 一次，小丘以唯一相邻消息 `seq 520→521` 回答；inbox `delivered`、`attempts=1`，原生 reply 与 mention 精确指回提问者。答案给出 `npm install -g @mininglamp-oss/octo-cli` 和 `octo-cli --help`，附两个固定提交的 GitHub 官方 README 来源，两条链接 `HEAD 200`。生产日志对此次测试标记、答案安装命令原文均为 0 命中，HTTP body 字段为 0。验收只覆盖该 CLI 安装问答和对应日志路径，不能推断所有问题、模型 provider 或错误分支均无泄露。

**候选与回退边界：**前两轮 `e9b3f826`、`b0740b68` 隔离候选分别被合成隐私哨兵阻断，均未部署；它们各约 592 MB 的备份留作回退／证据。`b0740b68` 曾在错误 `agent_id` 经 QA handler 与 GORM Warn 输出原文，已由 #45 修复并在第三轮验证。第三轮候选容器、网络、隔离文件和临时源码已清理，测试 CLI profile 已登出；新的约 592 MB 备份与校验结果保留在服务器私有 `/home/mlclaw/agent-data/operations/privacy-log-release-43030dd1-20260923T073322Z/`。隔离候选未连接模型／Milvus，**没有完成 PDF 全流程或真实知识检索回归**；生产也只做了上述一个 CLI 安装场景，三条错误状态的 GitHub 文档来源没有重跑。多源证据计划、选取范围预览、单仓库双用途与按文件恢复仍未实现，见 [实施计划](OCTO-PLAN.md)。

**OpenClaw 收口：**五个旧公共 Agent 已经通过原生 Gateway 停用并核对备份；现仅私人 `xiaoqiu` Agent 运行。计划任务从 13 条收敛到 3 条，六个 Bot account 恢复为启用／运行且原私聊绑定等值，并显式核对路由；旧插件停用。12 个旧 AgentDir 仍有 38 个打开文件句柄及 12 个活跃 lease，且 `main` 使用 `authInheritance`，因此保留目录及所需凭据；**停用 Agent 不等于可删除它的文件**。本轮未改变公开 Octo 小丘的 WeKnora 接入边界。

**清理与空间：**服务器此前执行已核验的低风险清理，释放 `1,890,164,209` 字节数据盘空间及 `554,033,152` 字节根盘旧镜像空间；失败候选另清理约 1 GiB 隔离数据，保留备份与阻断证据。运行容器挂载、知识库数据、私人 OpenClaw 运行目录和发布回退材料保留。桌面原有 23 个旧 Git 工作树和 6 个 Junction 空壳目录已定点清理，未合并的分支仍保留；只读回查只剩主仓库和独立的 `octo-weknora-answer-quality-20260922` 克隆。该独立克隆的删除被自动审批以 `blocked by policy` 拦截，因此保留，不借其他命令绕过。根盘当前约 16 GiB 可用；`containerd` 仍是需单独监控的容量风险，不能直接删除其数据目录或向当前不兼容的 XFS 数据盘强迁。

## 历史发布：4b（2026-09-23 13:29 后）

生产后端已从 `3d58f986` 切换到合并提交 `4b2e84f882c8ba0e74619ba49c4bca348c31d484`，运行容器的不可变 app Image ID 为 `sha256:861b0f3d…`；前端代码未变，继续使用已验证的 `3d58f986` UI Image ID `sha256:1406a7f1…`。本轮一次发布包含已合并的 [PR #35](https://github.com/90le/octo-weknora/pull/35)（文档 blob 断流诊断与有限重试）、[#37](https://github.com/90le/octo-weknora/pull/37)（Octo 回答的来源传递）和 [#38](https://github.com/90le/octo-weknora/pull/38)（OpenAI 兼容模型通道的常规日志脱敏与分段计时）。三项均已通过 Linux 定向测试和 GitHub Go CI／lint；上线后健康 200、前端 200、未认证知识 API 401，登录管理页仍可见 4 个知识库。PostgreSQL migration `105` 非 dirty，4 个活动知识库、60 条活动来源和 4 条问题保持不变，切换时运行中同步及解析均为 0；Octo 小丘通道已在新 app 初始化。

隔离候选与生产运行容器的 app Image ID 均为 `861b0f3d…`。候选使用独立恢复数据库与 internal 网络，IM、报表、来源计划均关闭；健康 200、未认证 401、认证系统信息返回完整提交 SHA 和 migration105，认证知识库列表为 4 个；解析引擎报告 Anydoc 可用。**本轮未在隔离候选重新完成 PDF 全流程入库**，因为恢复库现有 KB 的向量／关键词索引需要候选网络未连接的模型与 Milvus；上一版使用同一已核验解析运行资产完成过合成 PDF 解析。生产备份（Compose、私有 env、PG 一致性导出及应用文件哈希）在数据盘私有 `/home/mlclaw/agent-data/operations/p1-backup-4b2e84f8-20260923T045323Z`，旧 app 镜像保留；Milvus／Neo4j 卷不在该次文件快照中，本轮代码无迁移或索引结构变更。

**上线后来源验收（13:17–13:18 CST）：**本人在「Steward 联调群」询问 Node 环境安装 Octo CLI，Bot 唯一回复给出正确 npm 命令，并在 Octo 实际出站消息中附 3 条 GitHub 固定提交资料链接；逐条 HTTP 可达且内容支持安装断言，本人在客户端确认能够点击打开。原生 `reply` 和 @ 均指向本人消息，inbox `delivered`、`attempts=1`；Agent 的结构化知识引用数从旧轮 0 变为本轮 3。[#37](https://github.com/90le/octo-weknora/pull/37) 的这条群内来源展示路径通过。该轮耗时约 46.7 秒、总 75,116 token；首轮模型因原 Agent 显式 `max_completion_tokens=4096` 返回 `finish=length`，两个可能不完整的工具调用被安全拒绝，下一轮重试后完成。本人随后在管理页把**仅 Octo 小丘**的上限调至 `8192`，数据库已回读；新设置是否使首轮工具正常完成、总成本下降，待下一条真实问题观察，不推断已有改善。

**仍未完成：**三条 GitHub 文档来源仍为 `error`，[#35](https://github.com/90le/octo-weknora/pull/35) 的有限 blob 重试不是可续传仓库同步方案，本轮没有擅自修改整仓选取范围或批量重跑。常规 OpenAI 兼容模型请求日志已去掉正文，显式专用调试文件权限已收紧；但上线后只读审计发现 AgentQA／共用 Pipeline INFO 仍记录用户问题或检索内容，正另行修复，不能宣称全链路日志已脱敏。其他模型 provider 的错误文本路径也需单独审查。正式的多源证据计划、范围预览、单仓库双用途和按文件恢复仍在 [实施计划](OCTO-PLAN.md)，不得宣称已实现。

## 历史发布：3d（2026-09-23 12:03 后）

PR [#34](https://github.com/90le/octo-weknora/pull/34) 已合并为 `3d58f986`，生产 app/UI 已切换到该提交的镜像。运行容器实际镜像 ID 为 app `sha256:003e148a…`、UI `sha256:1406a7f1…`；隔离候选和生产 app 已核对为同一 app ID，候选系统信息返回完整提交 SHA。第一次切换遇到并行构建覆盖可变标签，已立即回退旧镜像，查明候选实际 ID 后改用唯一验证标签重新切换；不能把最初的标签或被覆盖的 `5327805…` 当作发布依据。

生产回读：健康接口 200、前端 200、未认证知识 API 401；已登录管理页列出原有 4 个知识库。PostgreSQL migration `105` 且非 dirty，4 个活动知识库、60 条活动来源、4 条问题保持不变；切换时运行中同步与解析均为 0。Octo 小丘渠道已在新 app 初始化，`Steward 联调群` 的 `GROUP.md v6` 仍在线。隔离候选经恢复数据库、禁用 IM／计划任务后完成管理员登录及 4 库读回；合成 PDF 由 Anydoc 解析并产生 1 个正确分块。专用合成库未配置摘要模型，故不宣称摘要流程通过。

**上线后真实群验收（12:00–12:03 CST）：**本人先问“Octo 和 Loop 的关系是什么？”，再问“详细一点”；Octo 原生消息顺序为用户 514→Bot 515、用户 516→Bot 517，各有唯一相邻且切题的回复，不再触发无关发布兜底。两条 inbox 均 `delivered`、`attempts=1`；Bot 出站的 `reply` 分别绑定正确的用户消息，`mention.uids` 均为真实提问者 UID。第一轮约 98 秒、总 39,569 token；第二轮约 31 秒、总 42,603 token。每轮仅调用一次 `knowledge_search`，各约 2 秒。第一轮首次模型请求约 78.5 秒，其中约 77.5 秒处于收到可处理响应流之前；首次请求实际 prompt 为 14,628 token，不能由此断言网关排队、网络或模型预填充哪个负责。第二条用户入站 `Quote` 为空，说明这次验证了同会话追问，**未验证用户原生引用上一条 Bot 消息的路径**。Agent 保存的文本含知识库引用标记，但 Octo 实际出站删掉了标记且无来源 URL，**群内来源展示未通过**；答复内容正确不等于引用质量和时延已验收。

发布前的 Compose、私有环境、数据库一致性导出与应用文件快照及哈希位于服务器私有 `/home/mlclaw/agent-data/operations/production-rollout-3d58f98-20260923T033441Z`；构建及隔离候选证据位于同一数据盘的 `answer-turn-scope-candidate-3d58f98`。未改动既有知识库权限、来源配置或私人 OpenClaw。旧 c3 镜像及快照保留，当前补丁无数据库迁移。

PR [#35](https://github.com/90le/octo-weknora/pull/35) 已合并为 `20c930b4`，只改进 GitHub 文档 blob 的断流诊断与有限重试，**尚未部署生产**。截图中的 `octo-android`、`octo-server`、`octo-web` 三条文档来源仍为 `error` 且无成功同步时间；同仓库的源码只读来源成功不代表文档已入库。只读诊断发现这三条文档来源均选整仓，Android 当前 987 个候选中有 976 张图片，Web 828 个候选中有 604 张 PNG；文档模式逐 blob REST 获取，长任务任一读取失败便整源失败且不能跨运行续接。当前树响应均未触及大小或截断限制；旧日志没有具体失败阶段，不能断言每次断流的精确文件。60 条来源均配置六小时整点触发，后续须处理错峰、范围预览、可恢复进度和双用途共享获取，详见 [实施计划](OCTO-PLAN.md)。

## 历史接手快照（2026-09-23 10:13 CST）

项目根目录是 `octo-weknora`，公开知识助理「Octo 小丘」由 WeKnora 原生 Octo 渠道接收；私人「小丘」仍属独立的 OpenClaw 运行时。目标是让群、子区在授权范围内使用 RAG 文档、只读源码和正式 Release 证据，完成有来源的回答、问题登记与知识运营；不恢复旧 Steward 中继。本次接手只更新文档，没有部署、同步来源、恢复文档或发送群消息。

| 核对项 | 本次只读证据 | 结论边界 |
|---|---|---|
| 代码与发布 | 接手前本地 `main` 干净，HEAD `62faca3`；线上 app/UI 均运行 `c3c1f0c` 镜像 | `62faca3` 是接手前的文档合并基线；本页后续改动是否发布应以 Git 提交和线上镜像回读为准 |
| 数据 | PostgreSQL migration `105`（非 dirty）；4 个未删除知识库、1158 条知识记录 | 1156 `completed`、1 `failed`、1 `finalizing`；`finalizing` 是本次时点状态，发布前必须重新核对 |
| 来源 | 60 条活动数据源：只读源码 30 条均 `active`，文档入库 27 条 `active`、3 条 `error` | 这 3 条尚无成功同步时间；错误记录早于 `c3c1f0c` 上线，未证明新版重试仍失败 |
| Octo 业务 | 5 条区域、7 条查询绑定、0 条独立维护授权；4 条问题（3 `open`、1 `resolved`） | 记录数量不证明每条绑定或权限都已完成新版真实群验收 |
| 运行与空间 | app/UI 容器运行；Octo daemon `running`；根盘约 35 GiB 可用，2 TiB 数据盘约 21 GiB 已用 | 本次没有复测 OpenClaw Gateway 的 WebSocket 或 Octo 群内收发；容器在线不代表业务验收 |

**待复核与未完成：**

1. 在「Steward 联调群」用现有获授权身份验收一条 Android/Web 正式版本问题和一条 `codex-channel-octo`、`cc-channel-octo`、`hermes-channel-octo` 多仓问题；逐条核对来源链接、内容边界、耗时和工具调用。新版本上线后尚无这两条真实群验收证据。
2. 分别重试 `octo-android`、`octo-server`、`octo-web` 的 GitHub **文档入库**来源，先看单条失败原因及旧版本是否保留，不批量重跑 60 条。`What's New` 在本次检查时为 `finalizing`；确认其结局后再安排发布或恢复。
3. `DAP_EVENTS.md` 是 1 条失败的 GitHub 文档候选；恢复运行记录为 0。管理员先预览、核对可恢复范围，再决定是否显式执行。URL 类型资料不属于这条 GitHub 文档恢复入口。
4. `AIBP 产品知识库` 当前有 0 条知识记录，尚不能宣称已具备真实 AIBP 产品问答能力。真实资料的接入范围需按维护授权和来源生命周期单独完成。

已知界面限制：恢复任务启动后，当前弹窗会持续轮询该 run 并禁止普通关闭；浏览器刷新后尚无“按来源找回正在运行的 run”的页面入口。后端任务和记录仍持久化，管理员应从任务记录核对结果，不能因页面状态丢失重复启动。

发布、数据、来源与空间数字来自本次服务器容器清单、PostgreSQL 只读查询及 `df`；操作目录 `/home/mlclaw/agent-data/operations/production-rollout-c3c1f0c-20260922T150759Z` 保存上一轮发布证据，含私有配置和备份，不复制到仓库。代码交付见 [PR #30](https://github.com/90le/octo-weknora/pull/30)、[#31](https://github.com/90le/octo-weknora/pull/31)、[#32](https://github.com/90le/octo-weknora/pull/32)；其通过的 CI、隔离候选和容器检查属于相应版本的历史证据，不能替代待做的真实群验收。

### 2026-09-23 群问答故障与规则更新

「Steward 联调群」的一轮“Octo 和 Loop 的关系是什么？”在 WeKnora 内先成功检索并生成关系答复，最终却被固定的“缺少发布记录”答复覆盖。生产记录显示一条用户消息、一次 Octo 投递、两次成功检索、两次多余的证据重试；这轮共耗时约 23.7 秒、累计约 13.3 万 token。根因是原问题被判为普通问题但未留下分类标记；群 `GROUP.md` 被加入模型查询后，引擎对混合文本重新分类，将群规则里的“发布”误当成本轮提问意图。不是知识库零命中或消息发送重试。代码路径见 `internal/answerevidence/contract.go`、`internal/im/execution_scope.go`、`internal/application/service/session_agent_qa.go` 与 `internal/agent/engine.go`。

已通过 Octo 原生接口把该群 `GROUP.md` 从 v5 更新到 **v6**，只替换了触发误分类的“发布”措辞，并补充“可回答时先简明回答、要求详细时沿原话题展开”；写入后按全文和版本回读，原文备份留在本机私有目录。此规则更新立即生效，但不能替代程序修复。程序修复已在 Windows 契约测试和隔离 Linux 的四个相关 Go 包定向回归通过；本节所述时点**线上代码尚未切换**。其他群、子区及知识库配置未改。已知边界是仅问“这条呢？”并引用上一条发布信息时，仍需后续结构化追问解析。10:38 CST 的只读复查还确认运行中的来源同步和资料解析均为 0，先前 `finalizing` 的 URL 资料已转为 `completed`。

## 历史发布范围：c3（2026-09-22）

线上知识助理后端与前端已发布为 `c3c1f0c37090a1eaa55d56a50314cc48d88f9173`。本次合并交付了 [PR #30](https://github.com/90le/octo-weknora/pull/30)、[PR #31](https://github.com/90le/octo-weknora/pull/31) 和 [PR #32](https://github.com/90le/octo-weknora/pull/32)：管理员显式的重启恢复入口、按证据类型分流的回答链路，以及 GitHub 批量来源和源码快照的规模化与恢复边界。

部署当时回读一致：PostgreSQL migration **105**、知识库 **4**、活动数据源 **60**、运行中的同步 **0**、待解析知识 **0**。这组数字是 9 月 22 日的发布时快照；9 月 23 日的现值以上方接手快照为准。后端受保护 API 返回预期认证拒绝，前端容器返回 HTTP 200；发布当时 OpenClaw Gateway 的 WebSocket probe 与 Octo daemon 均在线。发布前的 Compose、私有环境快照与 PostgreSQL 一致性导出保存在服务器私有运维目录，未写入仓库。

### 回答证据链

- **最新版本、更新内容、发布日志**：只查询授权仓库的 GitHub 正式 Release。普通分支、tag、package 版本或 RAG 片段不能替代正式发布记录。
- **点名的源码仓库与实现机制**：每个仓库分别进行受权快照检索和固定提交文件读取；引用固定 GitHub commit 与行号。一个仓库的 README 不能推断另一个项目的能力。
- **产品说明、FAQ、业务资料**：由文档 RAG/Wiki 使用真实段落证据回答。
- **资料不足**：只有相应受权查找真正完成后才可以回答“无法确认”；检索超时、未同步来源或零命中不会自动登记为知识缺口。

已实现的服务端控制包括：三仓 channel 问题的并行预检；预检完整后有限的定向源码读取预算，防止同一问题重复几十次读取；官方 Release 来源由 Octo IM 自动附加，且遵守 Agent 的 citations 开关。它们不是单靠提示词约束。

### GitHub 来源与规模化

- 源码只读快照与文档 RAG 入库保持分离；源码模式不会进入解析、分块或向量化路径。
- 同一提交只做远端 ref 核对并复用完整快照；提交变化时复用未变 Git blob；个别本地对象丢失时重新读取该 blob，不把整个来源永久标为异常。
- 批量选择超过 20 个仓库时，页面按每 20 个顺序提交，汇总 created / existing / failed / skipped；失败后的其余批次明确显示“未提交”，不会自动重试。
- HTTPS、SSH、`github.com/owner/repo` 与 `.git` 旧格式被规范化为同一仓库身份，防止重复来源。
- 未同步或不可读的来源会让全局源码搜索标记为“不完整”，不能把零结果当成穷尽结论。

### 当前可用的恢复入口

“知识库 → 数据源”的 GitHub **文档入库**来源菜单中，管理员可打开“恢复中断文档”。该入口只在点击后预演本来源已存储的文件候选；展示可恢复、排除和阻止原因；需要显式确认才启动；运行中只轮询该 run，当前弹窗不允许普通关闭。它不访问 GitHub、不发起普通同步，也不会显示路径、密钥、确认 token 或原始存储错误。当前没有自动执行任何恢复。刷新页面后的状态找回限制见上方接手快照。

### 尚待真实群回归

代码、Linux CI、隔离候选和线上容器健康均已通过；最新发布后还没有从“Steward 联调群”的真实成员身份重新发送版本问题与三仓项目问题。因此不要把当前容器健康写成这两条群消息已经验收。后续仅需用既有联调身份发送两条最小问题，核对回复、链接和工具次数；不需要新建账号、重新配置 Token 或批量测试。

## 历史记录（2026-09-21 及之前，以下各节均非当前状态）

以下各节保留当时的部署、清理、测试和限制，供追溯使用。其中“当前”“下一工作包”均指原记录日期；项目现状和下一步只以上方接手快照与 [实施计划](OCTO-PLAN.md) 为准。

独立知识产品已部署：后端与前端当前均为 `6aa0fe5ea116`，对应 [PR #18](https://github.com/90le/octo-weknora/pull/18) 的合并代码树。PostgreSQL 迁移为 **104**，SQLite 对应 **25**。公开「Octo 小丘」只有 WeKnora 原生接收端；仅删除了旧接入中该 Bot 的账号、路由与凭据项，私人小丘及其他五个 Bot 的配置、凭据逐项不变，原服务仍运行。

本轮修复了 GitHub 批量源码接入的两个实际问题：首次带凭据同步会在 clone 目标目录尚不存在时创建认证辅助脚本，现已改为在受限父目录短暂创建并在命令结束后删除；重复批量提交会按规范化的 `owner/repository + 用途` 返回“已存在”。旧的 HTTPS、大小写和 `.git` 形式也会识别为同一仓库，旧记录缺少用途时按“文档入库”兼容。新请求仍必须明确选择用途。

正式切换前，隔离候选使用一条已有的加密 GitHub 凭据完成首次源码同步（214 个文件）、固定快照发布和第二次同版本跳过（214 个文件）；大小写与 `.git` 变体返回同一已有来源，文档入库没有被误判为源码重复。候选库关闭了所有外发渠道和周期任务，正式库未写入测试数据。正式切换后，原先 20 条 askpass 失败来源与 10 条“已创建但未同步”来源均原地首次同步成功；没有剩余 askpass 错误或内部快照路径泄露。一个独立的历史文档入库来源仍因 GitHub 403 保持 error，未擅自删除或改写它的凭据。

GitHub 批量接入、公开 ref 兼容和浅克隆源码缓存已通过 [PR #11](https://github.com/90le/octo-weknora/pull/11)、[PR #12](https://github.com/90le/octo-weknora/pull/12)、[PR #14](https://github.com/90le/octo-weknora/pull/14) 合并并完成正式切换。隔离候选已验证首次同步、重复同步跳过未变文件、固定提交按行读取、删除来源后清理生成缓存，且不修改正式资产；正式切换后既有资产数量和数据库迁移均回读一致。

历史 [PR #16](https://github.com/90le/octo-weknora/pull/16) 修复了批量 GitHub 接入的空排除规则错误，增加同提交快照复用，以及来源内容的预览／确认删除链路。其发布时的资产计数是历史快照，不应替代本页顶部的当前发布状态。

WeKnora 运行时不读取旧 Agent 的配置、问题数据库、技能或工作目录。原始知识资产保留；旧聊天和问题记录没有自动迁入。原有四个知识库、两个主群及三个子区的接入配置保留，Steward 联调群增加现有 CLI 知识库用于问答。

## 已交付与证据

| 工作包 | 已交付 | 本轮验证 |
|---|---|---|
| 原生接管 | Bot 身份、私聊名单、群／子区独立上下文、原生 @ 与完整引用 | 真实群问答及重启后再次问答通过；本人真实私聊收发与授权知识范围已核对，名单只保留本人 UID |
| 配置页面 | 独立“渠道接入”“问题反馈”页面，报告与周报统一入口，联系人跟随知识库 | 真实浏览器导航、问题处理保存、联系人保存、报告预览通过；1366px 完整显示问题列，768px 筛选换行、表格可横向滚动 |
| 绑定与权限 | 多对多查询绑定、独立维护授权、子区继承及问题汇总、数据库目录登记 | 群内改名、解绑保留管理权、重绑实际成功；主群未泄漏子区答案；陌生 Bot 抢确认不执行 |
| 知识维护 | 服务端预览／原样确认命令，管理员专用草稿定位、完整正文与状态读取 | 真实保存草稿；公开检索不可见；自然请求定位后发布，解析完成且可检索；其他人不能接管确认 |
| 问题与报告 | 缺口、Bug、建议、来源及责任人、状态历史、文本／文件报告、原生周期任务 | 未要求“登记”也成功登记明确知识缺口；Bug 补充事实后登记；HTML 原生附件实际下载并核对内容、字节数；定时报告实际发送 |
| 来源组合 | RAG、资料文件夹与只读源码组合；页面选择已授权目录；固定版本引用 | 新 Markdown 原生入库成功；子区读取 RAG 与源码；源码计算由 42 更新至 54，回答引用新快照；旧快照仍读到旧内容；临时来源故障未丢失已有版本 |
| 资料空间与目录授权 | 独立资料空间页、挂载发现、目录树授权、数据源安全选择、同步撤权重核 | Linux 目录／权限／撤权目标测试、前端 19 项检查、类型检查、生产构建与 GitHub CI 通过；默认目录清理后只保留“资料接入目录”及其一个工作区授权 |
| 稳定性与删除 | 持久消息去重、有限发送重试、权限撤销复核、通用删除重试、固定删除回执 | 重启后配置与问答保留；两临时库 Milvus 向量均为 0，PG／Wiki／资源绑定与任务残留核对；原生删除前后测试原稿三文件哈希完全相同 |

代码检查：对应发布提交的后端格式／vet／全量测试／构建、前端测试／类型检查／构建、Docker、适配器和隔离专项均通过。另跑了定向的草稿撤权、确认幂等、跨范围拒绝、上传凭据隔离、断线、报告恢复与目录权限回归。静态检查、隔离启动和真实 Octo 行为分别留证，没有把健康检查当作全部验收。

## 资料空间与安全目录授权（2026-09-21）

- 资料输入链路已调整为“只读挂载 → 资料空间 → 当前工作区授权目录 → 知识库数据源”。“发现已有挂载”只扫描容器内 `/source-roots` 的一级真实目录；它不会挂盘、创建目录、授予权限、创建数据源或同步文件。
- 平台管理员在“知识库 → 资料空间”中管理已有空间、健康状态和当前工作区授权目录；数据源管理员只从已授权目录树选择范围。空间统计是全工作区基础设施数据，目录用量只显示当前工作区，页面不接受任意宿主机路径。
- 服务端只为系统管理员开放空间发现和授权接口；工作区数据源管理员的目录浏览只返回本工作区启用授权下的相对路径。路径穿越、绝对路径、链接及目录替换均拒绝，使用中的授权目录不能删除。
- 同步在取数后、每 16 个文档或最多一秒、流式检查点和源码快照发布前复核来源、配置、暂停状态和当前目录授权。撤权后不会提交新的游标或快照；已经完成的单项入库不回滚，已入库 RAG 文档也不会因停用目录自动下架。
- 发布前保存了 Compose、私有配置、PostgreSQL 一致性导出和应用文件快照；候选健康、资产、数据库版本和挂载均回读。发布过程中检测到外部新增数据源，首轮脚本自动回退；随后改为保护已有资产、允许独立操作追加后成功切换。没有删除或回滚这些新增资料。
- 初始完整镜像构建先后遇到第三方 BrowserSkill 补丁源漂移和 PyPI 超时。最终候选从当前已验证运行层继承 Anydoc、操作系统依赖和 BrowserSkill；重新编译本提交的 WeKnora 二进制、配置、迁移和前端。BrowserSkill 二进制、扩展包和许可证哈希与旧线上镜像一致。构建脚本同时补充模块文件与许可证清单的 LF 规则，避免 Windows 归档把版本或校验文件名附加 CRLF。
- 已实测：后端／前端健康端点为 200，两个新 API 路由均返回认证拒绝而非 404，应用无启动 panic，所有五个既有 `/source-roots` 只读挂载仍存在。仍未把这一轮健康和接口检查写成“群内问答、资料解析、所有角色和所有屏幕尺寸均已重新验收”；这些既有能力没有在本次改动中重写。
- 随后按明确清理要求，移除了 Octo CLI 源码／文档和 OpenClaw Octo 插件源码／文档四个默认只读挂载：Compose、`DATASOURCE_LOCAL_ROOTS`、资料空间记录和三个对应授权已同步清除。没有本地文件夹数据源引用这些目录，服务器原始仓库和文档目录没有删除。运行容器现只保留 `/source-roots/inbox`，资料空间与授权目录也各只剩“资料接入目录”一项；重启后的健康检查通过。

## GitHub 批量接入与源码同步发布（2026-09-21）

- 组织／作者仓库发现与批量接入已作为原生数据源页面的一部分上线。发现不会创建来源或同步；一次最多明确创建 20 个仓库，每仓库仍是独立数据源，分别保有权限、日志、失败状态和只读快照。
- 源码模式改为每个数据源独立的受限浅克隆 Git 缓存。首次同步取得当前提交需要的目录对象，不拉取完整历史；后续同步复用已有对象和此前快照。此前的归档下载是历史实现，当前发布不再依赖它，也不使用会在代理链路中按需补取树对象的 partial clone。
- 隔离候选使用公开 `Mininglamp-OSS/octo-cli` 的 `README.md` 完成真实 Git 连接、首次同步、第二次未变跳过、固定提交读取与来源删除后的缓存清理。候选应用和前端健康均为 200，候选数据库来自正式库的一致性只读导出；通道和周期任务在候选库中关闭，正式库未写入测试数据。
- 正式替换只重建应用和前端；运行后两个入口均返回 200，`TMPDIR` 为持久数据卷内目录。回读保持 **4** 个知识库、**115** 条知识、**9** 个数据源、**5** 个范围与 **7** 条范围绑定不变，迁移仍为 **104**。OpenClaw Gateway、Octo daemon 和原有两个沙箱也均恢复在线。
- 资料输入、只读源码快照、Git 缓存、应用临时目录、构建缓存和发布证据均在 2TB 持久数据卷。Docker 数据根目录也已在该卷；系统 `containerd` 不能迁移到当前数据盘，因为其 XFS 缺少 overlayfs 所需的 `d_type`。在提供兼容的 `ftype=1` 文件系统前，`containerd` 保持系统盘，禁止再次尝试迁移或格式化现有数据盘。系统日志已设置 1 GiB 上限并保留至少 10 GiB 根盘余量。

## 来源生命周期与同版本复用发布（2026-09-21）

- 批量创建在未填写排除规则时不再写入 `exclude:null`；历史空值按默认排除目录兼容。超过 100 条排除规则会在批次开始前一次性拒绝，而不会把同一个配置错误重复显示在每个仓库上。
- 同一数据源解析到相同提交时，仍先检查远端 ref；确认未变后会验证本来源私有对象并整体复用快照，不再 clone、fetch、遍历 Git 树或重新读取 blob。对象缺失或损坏时自动走完整同步。不同知识库、不同数据源仍不共享缓存或源码对象。
- 删除入口改为先给出影响预览。默认“仅移除来源”，保留已入库知识；危险选项需要短时、签名、绑定操作者和候选集的确认令牌，只删除 `tenant + KB + datasource_id` 精确匹配的生成内容。确认令牌同样区分不同 API Key。
- 主文件与提取图片都通过资源绑定引用计数决定物理删除；仍被其他内容引用的资源保留。旧 raw 文件路径没有足够归属证据时，危险清理会拒绝执行，来源和知识都保持原样，等待单独审计。
- 候选 app／UI 均返回 200；批量创建、首次同步、同提交二次同步、删除预览和无内容来源的确认删除均通过。正式 app／UI 返回 200，OpenClaw Gateway 与 Octo daemon 保持 active，迁移仍为 104。完整候选与回退证据仅保存在服务器私有运维目录。

## 界面职责

- **知识库**：资料、数据源、只读源码、发布状态、使用范围与联系人。
- **渠道接入 → Bot 与私聊**：Bot／Agent、启停、实际连接状态、私聊授权与消息处理记录。
- **渠道接入 → Octo 群与子区**：真实父子结构；优先显示可查询的库和维护许可，连接 ID 和角色排查折叠。
- **问题反馈 → 问题列表**：来源、提问人、负责人、状态与处理历史；UID 留在详情。
- **问题反馈 → 报告与周报**：按真实条件预览和导出，周期发送单独配置。

设置页旧书签会跳到相应新页面。旧桌面控制台入口已改为当前后台指引，旧代码与文档作为历史资料保留。

## 本轮修复的真实阻塞

- 文件上传旧接口已经退役；改用官方预签名 GET → 对象存储 PUT → 原生附件消息，存储上传不携带 Bot Token。
- 宽泛业务工具导致模型遗漏关键参数；拆为专门的配置、联系人、问题、维护文档查询、预览、确认和报告工具，共用同一权限与执行服务。
- 预览提示“确认创建”与服务端确认协议不一致；现在返回可直接复制的原始确认／取消命令，错误提示不再误称权限丢失。
- 管理员无法通过公共 RAG 定位草稿；增加仅维护者可用的文档查询，普通 RAG 继续排除草稿。入口权限与执行时权限同时复核，延迟答复也检查维护权撤销。
- 查看 Bot 身份曾误用注册接口，官方接口会更新 IM Token；现在保存与当前凭据绑定的已核验身份，页面只读。实际连续读取、打开配置及重新核验后，连接时间戳保持不变。
- 区分临时断线与互踢／鉴权失败；展示实际运行状态，启用不再等同在线。报告中断标记结果不确定，不自动重复发送；重试耗尽立即可查。
- 补上非 Redis 原生任务队列的周报处理器注册，以及同步重试成功后清除当前日志旧错误。
- 删除最后一个库时普通答复权限已消失；仅允许与本次可信确认绑定的固定回执“删除请求已提交，后台清理中”，不放回模型正文或检索内容。

## 清理与交接边界

两个临时知识库、四条测试问题、临时周期计划、测试目录登记与子区接入已清理。临时维护白名单和建库开关已恢复；测试 Bot 只保留原查询许可。隔离环境七个容器、两个独占网络及约 819 MiB 数据副本、临时隧道和凭据已移除，验收记录保留。没有删除 Octo 群或既有子区。原生删除证明未损伤原稿后，才由运维删除本轮生成的三个测试输入文件。两条原生软删除数据源历史、消息处理与操作审计按原生规则保留。

固定测试资料中，44 条来源经内容与 Git 对象核对后补充了可信版本链接；12 条因缺少可核对原稿或内容不一致而保留现状，没有编造对应关系。完整物理文件归因缺少删除前的全部路径清单；本轮只对已知对象核对，不声称全盘历史残留均已清除。

本人已实际发送私聊消息；服务端核对真实 UID、direct 会话和已投递回复，查询可用资料时仅显示获授权的 CLI 知识库，无需再要求固定测试句式。未做大并发压测或完整辅助技术兼容审计；角色、私聊隔离和重启保护另有自动化验证。当前保留的是已有固定版本测试知识，真实 AIBP 产品资料仍需由维护者提供。

本轮不增加 OpenViking、Loop 中继、源码向量化、LSP、强制卡片或另一个管理平台。文件上传／外部同步沿用原生生命周期；需要审核的资料使用文字草稿或尚未对外绑定的准备库，不能把所有上传文件一概说成“待审核草稿”。

## 历史发布与运维记录

以下记录是历史证据；当前状态以上文及后续本轮验收记录为准。旧文中的待迁入、OpenClaw 编排等描述已被 2026-09-20 的独立接入方案取代。

## 知识图谱引擎已启用（2026-09-16）

- 按 WeKnora 官方 `KnowledgeGraph.md` 接入持久化 Neo4j 2025.10.1，服务名 `weknora-pilot-neo4j-1`，Bolt 仅在 Compose 内网提供；Browser HTTP 仅映射宿主机回环地址 `127.0.0.1:17474`，经持久 SSH 隧道访问。数据目录为服务器 `knowledge-services/weknora/pilot/neo4j`，凭据只保存在服务器私有 `.env`。
- `NEO4J_ENABLE=true`、`NEO4J_URI=bolt://neo4j:7687` 已注入 WeKnora 后端；重启后容器启动成功，Neo4j 认证和 WeKnora 系统页均显示 `Neo4j`。Milvus 向量与 PostgreSQL 业务数据不受影响。
- 仅对有资料的 `Octo CLI 官方项目知识库` 开启实体／关系抽取作为小范围验证；未修改 IMA，也未对全部测试库批量重算。最小 README 重解析完成，在 Neo4j 中验证 12 个实体节点、7 条关系。
- 查看入口：本机持久隧道提供 `http://127.0.0.1:17474/`，HTTP 200 已验证。浏览器登录及端到端查询尚未单独验收，不能由页面可达推断 Bolt 查询可用；Tailscale Serve 尚未启用。
- 图谱是按知识库的可选增强管道，不是向量模型或图片数据库。后续打开其他 KB 仍需配置抽取示例、确认模型成本并重新解析对应资料；不开启的 KB 继续使用 Milvus／关键词检索。
- 私有操作备份、配置前后快照和验证记录在服务器 `/home/mlclaw/agent-data/operations/neo4j-enable-20260916`；未把密码、知识内容或运行配置提交到仓库。

## 腾讯会议知识库创建回滚（2026-09-17）

- 用户确认本轮只需要创建和配置知识库，不需要代为上传资料；已删除误创建的 `腾讯会议蒸馏记录`（`4d8ec93b-f564-492c-93ac-69aff1ebd458`）及其 12 个数据源。
- 服务器同步副本 `/home/mlclaw/agent-data/knowledge/inbox/tencent-meetings`、28 条文档、1066 个分块、266 个资源和全部生成文件均已清理；Windows 原始目录未修改，仍有 28 个 Markdown 文件。
- Milvus 目标向量已核对为 0；现有 AIBP、Octo CLI、OpenClaw Octo 插件三个知识库重启后检索仍返回 200。应用调度器恢复为 0 条定时任务。
- 本次没有为会议资料保留 Wiki／图谱配置；后续重新创建时采用单一 KB 配置，RAG＋关键词检索和多模态解析优先，Wiki／图谱按需要开启。
- 私有清理计数记录：`/home/mlclaw/agent-data/operations/tencent-meeting-kb-20260917/cleanup-result.json`；没有创建备份。

## 已删除知识库残留清理（2026-09-17）

- 按用户确认，定点清理 `IMA`（`ca73d305-89e6-4dbf-a8c3-ab0049021113`）和 `测试知识库wiki`（`eb2c206f-a011-4175-ab01-20c57634f676`）。未创建备份。
- 两个目标的知识库、文档、分块、Wiki 页面／文件夹／版本、资源目录、资源绑定、数据源、IM 渠道和 Octo KB 绑定均已删除；7 个残留物理文件已通过受限 root 辅助容器删除。
- 目标 Milvus 向量为 0，Neo4j 目标节点为 0；现有 CLI／插件／AIBP 知识库保持不变，Neo4j 仍保留 CLI 测试图谱的 12 个节点和 7 条关系。
- 数据库硬删除回读全部为 0；清理记录仅保存计数和目标范围于服务器 `/home/mlclaw/agent-data/operations/kb-residue-clean-20260917/cleanup-result.json`。
- 这次是定点运维清理，尚未把同样的 Wiki／资源目录清理逻辑补回通用删除代码；后续删除流程仍应增加失败重试和完成校验。

以下版本号是各阶段历史发布证据，当前在线版本以上述统一发布为准。

## 首批 Octo 管理部署证据（历史基线）

- 后端：`d956fcaf5d43a7bff06b2e0375c824a47b6b3af2`；前端：`b705f549a9b5799baec2170e71949ce75b520f8c`。后续差异只有前端修复及可选构建脚本，没有混用不同后端协议。
- [PR #3](https://github.com/90le/octo-weknora/pull/3)、[PR #4](https://github.com/90le/octo-weknora/pull/4) 已合并。
- 最终 PR #4 CI：后端全量 vet/test/build、lint、隔离专项、前端全量测试/类型检查/构建和 Docker 镜像均通过。
- [后端 CI](https://github.com/90le/octo-weknora/actions/runs/34834162688)、[前端 CI](https://github.com/90le/octo-weknora/actions/runs/34834162735)、[隔离专项](https://github.com/90le/octo-weknora/actions/runs/34834162748)。
- 隔离副本启动和迁移、鉴权资料访问、正式切换后的健康与镜像回读已通过。三份既有接入资料的数量保持 0／35／21；这不是全平台知识库总数。
- Chrome 实测：官方名称刷新更新验证时间；真人群主识别正确；缺少 bot_admin 时不授管理权限；子区只展示对应绑定；KB 反查展示主群和对应子区。
- 修复并实测设置页面重复挂载；未知 Bot 管理身份不再显示为已确定的普通成员。
- 原生加密主密钥已在私有服务配置启用，Octo Token 加密入库、不回传浏览器。运行恢复材料和私有配置路径保留在私有运维交接，不进入公共仓库。

## GitHub 数据源交付（2026-09-15）

- 原生“知识库 → 设置 → 数据源”新增 GitHub，支持公开仓库免 Token、私有只读 Token、仓库／ref／文件或目录、原生调度与日志。保留目录，源码、隐藏文件、依赖、符号链接和子模块不作为文档入库。
- 增量按 blob 比较，固定 commit 来源进入原生检索引用。新文档处理完成后替换旧版；失败保留旧版，未完成候选不进入正式检索，重试复用候选。源端删除为可选；接口截断及选择范围改变不触发误删除。
- 真实验收发现原生异步解析会覆写稍后补写的来源与身份字段，已修复为创建时保存来源、处理结束后接管。首次失败没有算作通过；修复后原生全量检查恢复了同一候选，随后真实增量替换成功，只保留新版。
- 使用独立临时 KB 从本仓库同步进度文档，再通过正常合并更新主分支。观测到旧版在新版 finalizing 时仍存在，完成后旧版移除、来源固定到新提交。原生快速问答引用该新文档，正确答出 864 条／2560 维，来源不是旧资料。
- 文档详情增加原始仓库网址；预览／分块中的相对文档链接解析到相同固定提交，已在 Chrome 核对。不是任意目录或图片资源镜像；相对图片等更多资料类型仍需后续专项处理。
- [PR #5](https://github.com/90le/octo-weknora/pull/5)、[PR #7](https://github.com/90le/octo-weknora/pull/7) 已合并。当前后端 `4a4b89d779d6c87272316125e66ff06dd39b5361`，前端 `92535b791af024d1f267d469f04b01eaed9ecb0b`；两者差异仅前端链接修复，后端协议相同，无数据库迁移。
- 最新 [后端 CI](https://github.com/90le/octo-weknora/actions/runs/34925290877)、[前端 CI](https://github.com/90le/octo-weknora/actions/runs/34925290781)、[检索可见性专项](https://github.com/90le/octo-weknora/actions/runs/34925290810)、lint 均通过。隔离启动、受限 API 访问和正式镜像回读通过；未扩大原有 API Key 的范围，验收库由原生管理员页面创建。
- `--cache-parser` 已用于完整后端镜像构建并通过；下一次构建真实命中原生解析器缓存。发布辅助脚本的构建结果文件启动竞态已修复，没有跳过隔离检查。

本地代码工作树清理被自动审批策略拦截，未执行删除；主仓库提交已完成，工作树暂时保留。

临时验收 KB、数据源、资料和对话已通过原生操作清理并回读：原有 4 个 KB、5 个区域和 6 条绑定保持原状。最终检索保护上线后再次问答通过，未发送 Octo 群消息。

配置方法和限制见 [GitHub 数据源说明](internal/datasource/connector/github/README.md)。本次安全替换只作用于新增 GitHub 文档路径，没有静默重写其他连接器。首版限制 2000 文档、单文件 16 MiB／单批 64 MiB；超限或树截断明确失败。不是整库原子切换，也没有源码搜索工具。

## 服务器文件夹与源码交付（2026-09-15）

- [PR #8](https://github.com/90le/octo-weknora/pull/8) 已合并。原生数据源新增服务器文件夹，GitHub 增加源码与文本只读用途；全部文本语言后缀可读，已验证 JS／PHP／Python／未知扩展名。目录、文本搜索、按行读取、平台链接及固定仓库行号引用共用来源服务。
- 当时的公开源码实现通过 Git ref 广告和固定归档下载，不受匿名 REST 配额耗尽阻断；私有连接仍使用配置的只读令牌。该归档路径已由 2026-09-21 发布的浅克隆缓存替代；失败仍不发布部分快照。
- 原生 `source_browse` 工具已注册，接入 KB 授权、原生模型 ID 映射与引用协议。文件／标签选择不扩张为完整源码范围。Agent 前端增加工具选项，纯源码工具不强制配置重排模型。
- 实机：本地 5 个文本文件可读，密钥文件与越界符号链接被跳过；GitHub Octo CLI 整仓得到 207 个可读文件。两种只读来源存在时 RAG 文档数仍为 0。独立文档来源随后仅导入 README，原生解析完成。
- 临时只读 Agent 仅配置一个测试 KB 和 `source_browse`，实际读取 JS 函数后回答 calculateFee(100)=7，并生成文件／快照／行号引用。引用参数重复转义已修复，点击后打开正确代码。源码浏览页的 GitHub 链接固定提交并带行号。
- 本地源文件变更后手动同步产生新快照，日志为 1 个更新、4 个未变。旧引用仍对应原有快照。
- 后端镜像提交 `f175753a59d7c0420f3cb81cfd174f4a0a6e89e4`，前端 `74b889bd26e1d7cb53efb753a98ac9cd95b710bd`；差异仅前端，协议一致，无 DB 迁移。原 Milvus、模型配置、旧 KB／区域绑定保留。
- 最新 [后端 CI](https://github.com/90le/octo-weknora/actions/runs/34934031461)、[前端 CI](https://github.com/90le/octo-weknora/actions/runs/34934031362)、[隔离检查](https://github.com/90le/octo-weknora/actions/runs/34934031460)、[可见性检查](https://github.com/90le/octo-weknora/actions/runs/34934031418)、lint 均通过。过程中一个未修改的 SQLite MCP 并发测试曾出现 table locked，保留记录；后续全量检查通过，未屏蔽测试。

临时 KB、3 个数据源、文档、只读 Agent 和会话已清理；生成快照缓存已由原生删除流程清理，源目录中的本轮样例文件另行定点删除。原 4 个 KB、5 个区域、6 条绑定保留。

操作与接口见 [来源配置指南](OCTO-SOURCES.md)。本地 Git 远端引用有内容与可访问性核验，普通目录及未提交代码不冒充远端提交。

## 当前限制与下一工作包

来源基础能力和原生 Octo IM 身份／授权／会话隔离已部署，当前线上 OpenClaw 问答与私人小丘保持运行。下一工作包只在真实需求出现后推进：GitHub webhook、服务器目录变更监听、GitLab 源码快照、历史快照自动保留与配额回收，以及 LSP／符号索引。已软删除的历史来源内容也必须先出具精确预览和审计范围，不能由新删除入口自动批量扫除。它们均未实现，也不由本次健康检查推断为已验收。

复杂二进制资料解析、所有角色的真实群问答回归和所有屏幕尺寸仍分别记录和验收；管理页面预览不等于公共群问答已切换。

## Milvus 直接迁移已完成（2026-09-15）

用户确认全部现有 KB 为测试集，授权直接切换。复用已部署原生驱动，未修改后端二进制、未开发通用迁移平台。

- 在现有 Compose 中新增持久化 Milvus 2.6.11 单机服务；不发布宿主机端口，内部网络访问。独立于旧评估实例，数据目录位于现有数据盘。
- 四个现有 KB 与新库统一使用原生默认 Milvus（vector_store_id 为空，由 RETRIEVE_DRIVER=milvus 解析）。PostgreSQL 继续承载业务数据，旧向量和切换前数据库备份保留。
- 迁移 864 条、2560 维向量及文本／启用状态等元数据：CLI 518、Octo 插件 346，其他两库为空。没有重新调用向量模型生成这批向量；距离度量使用 COSINE，保留 BM25 文本检索。
- 两个有资料 KB 的实际 API 向量检索、关键词检索均通过；自然语言混合检索通过。Milvus 容器强制重建后仍为 864 条，API 再验通过。
- 5 条区域、6 条群／子区绑定逐行回读保持一致，KB ID 未变。OpenClaw Gateway 与 Loop daemon 保持 active；本次未要求真人重发消息。
- 修复部署中的评估服务 DNS 别名冲突、代理直连遗漏及 gRPC 解析后 IP 许可问题：长期实例使用唯一地址与固定内部 IP，定向允许连接，不放开整个内网。

本次不等于完整跨工作区权限回归、新文档解析或 Octo 对话端到端重验；这些边界按项目验收计划继续推进。旧评估实例未删除。恢复时不能忽略切换后新增资料；先暂停写入、备份当前状态，再按私有运维交接处理。

### Milvus 重复入口修正（2026-09-15）

此前一次性迁移同时登记了 DB VectorStore 和环境默认连接，形成两个相同入口，是部署配置错误。已备份后将四库统一恢复原生默认路由，软删除重复登记和零引用的旧评估登记；不删除向量、文档或群绑定。两库向量／关键词 API 再验通过，Chrome 设置页实测只显示一个 Milvus DEFAULT。旧评估容器仍未删除；设置页登记已清理。
