# 实施计划与接续入口

更新：2026-09-20。需求以 [需求地图](OCTO-REQUIREMENTS.md) 为准，完成证据仅维护在 [进度](OCTO-STATUS.md)。

| 工作包 | 交付 | 完成门槛 |
|---|---|---|
| P1 独立接管 | 原生 Octo 渠道、单一接收端、Agent、本人私聊、测试群绑定 | 真实群问答正确引用；旧接入仅移除公共 Bot；其他账号逐项不变 |
| P2 配置体验 | Token 身份核验、真实群／子区选择、双向绑定、私聊多库、服务器目录登记 | 从页面完成新库接入；日常配置无需 JSON；工作区切换不串数据 |
| P3 知识业务 | 澄清登记、联系人、草稿／发布、管理确认、查询／报告／文件及周报 | 当前真实身份与权限每次复核；草稿不泄漏；同用户确认；真实发送结果 |
| P4 可靠性 | 持久消息记录、有限重试、原始资料保护、通用删除清理、同步失败保留旧版本 | 隔离、重启、重复消息、失败回读和删除重试验证 |
| P5 收口 | 真实部署、界面检查、文档与过时入口清理 | 运行版本可追溯；未验证项明确列出；主任务一次性交接 |

执行顺序：先形成独立可用闭环，再并行补配置与业务；不等待旧功能照搬完成才接管。只允许一个公共 Bot 接收端。新接入失败时修复本系统，不静默回退旧链路。

验收分三层记录：代码与自动测试、隔离数据库候选启动、真实 UI／Octo 行为。编译通过或健康检查不等于全部业务验收。

本轮不增加 OpenViking、Loop 中继、源码向量化、LSP、第二套后台或通用 Agent 平台。Git webhook、目录实时监听、源码语义符号索引按真实需求后续增加。
