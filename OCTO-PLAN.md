# 实施计划与接续入口

更新：2026-09-15。范围以 [需求地图](OCTO-REQUIREMENTS.md) 为准；完成状态只维护在 [进度](OCTO-STATUS.md)。沿用原生数据源与前端设计；不承诺未经评估的工期。

## 阶段与工作包

| 工作包 | 需求 | 依赖 | 实际交付与完成门槛 |
|---|---|---|---|
| P0 基线替换 | R15 | 无 | fork 部署、草稿过滤补丁、退出旧后台、保留数据和恢复点；部署事实与业务验收分开 |
| P1.1 统一对象与授权 | R03/R04/R09/R11/R14 | P0 | 原生 KB／tenant／workspace 复用评估、来源和区域绑定数据库迁移、可信身份映射、查询和维护权限分离；后端拒绝越权 |
| P1.2 双向管理界面 | R04/R05/R09/R10/R12/R16 | P1.1 | 群子区层级与 KB 详情互跳；显示生效绑定、权限、真实名称；原生建库改名；解绑／删除影响明确 |
| P1.3 配置迁入 | R15 | P1.1/P1.2/P2.1/P2.2 | 预览映射可提前；实际切换须待来源与可信运行时就绪，备份、迁入回读；移除旧业务配置读取，禁止双写；不整目录删除运行时插件 |
| P2.1 来源可读闭环 | R02/R03/R16 | P1.1/P1.2 的管理基础 | 在原生数据源页补 GitHub 和获准服务器文件夹；路径规则区分资料入库／目录读取；源码不向量化；目录搜索、按行读取、固定版本引用；不等待 P1.3 完成 |
| P2.2 同步与故障处理 | R03/R15 | P2.1 | 定时与手动刷新，版本准备后切换，同源更新互斥，失败保留旧版；文件和索引状态分离；本地变化检测 |
| P3.1 问答与反馈 | R01/R02/R06/R07/R11/R13 | P1/P2 | OpenClaw 调用统一授权接口；澄清、未知、检索故障区分；缺口／Bug 登记、原消息关联、联系人；复用原生资料维护 |
| P3.2 运营界面与报告 | R05/R08/R16 | P3.1 | 反馈筛选、处理状态、备注和关联资料，按授权范围统计；输出格式按请求选择，不内置固定报告流程 |
| P4 发布收口 | R12/R13/R14/R15 | P1–P3 | 必要端到端验收、回滚／恢复说明、真实解析与渠道补验、旧残余按依赖清单清理、统一配置指南 |

## 当前下一步

2026-09-15：用户确认现有知识库均为测试集，采用一次性直接迁移，不开发通用跨引擎迁移平台。Milvus 接入与切换已完成，验证见进度文档。

下一步继续原生 GitHub 数据源与安全同步工作包，再补获准服务器目录、源码快照及混合路径读取。源码不向量化，Markdown／其他文档按路径规则进入 RAG。

Milvus 后续运维沿用原生向量存储设置；现有 KB 与新库统一使用原生默认 Milvus，避免环境连接与数据库登记重复。PostgreSQL 保留业务数据。已有库仍不支持普通编辑接口任意更换引擎；将来有真实生产跨引擎迁移需求时，再补增量、切换和回退机制，不将本次测试集迁移冒充通用功能。

编码前将工作包拆成对应 PR，PR 标明需求编号、数据库影响、验证证据和未完成项。同一阶段可以开发与部署连续推进，不要求用户逐步回复。

## 必要验收矩阵

| 场景 | 必须证明 |
|---|---|
| A/B 两个区域共用与隔离 | 授权共享可读；猜 KB ID 不可越权；记录仍各自隔离 |
| 子区继承与汇总 | 界面展示与实际查询一致；汇总不获得维护权限 |
| 身份与变更 | 原生用户／Bot 身份不可伪造；默认目标不授写权；解绑不删资料 |
| Git 新提交／同步失败 | 新提交可切换且引用正确；失败保留旧版；一轮读取版本一致 |
| 本地目录变化 | 不改原目录；禁止越界；变化可见且不夸大快照一致性 |
| 同步更新与编辑 | 新版解析失败时旧知识仍可用；人工修订不被静默覆盖；目录移除、过滤变化、真实源端删除有区分 |
| 混合来源 | docs 进入指定资料管道、src 保留目录不调用嵌入模型；嵌套目录及相对链接有效；跳过项有原因 |
| 原生页面一致性 | 数据源原有创建／编辑／日志仍可用；复用 SettingDrawer 与主题；键盘、窄屏、加载／错误态可操作 |
| 已知／未知／服务故障 | 有据回答；缺口登记真实；服务失败不制造虚假缺口 |
| 问题处理 | 原消息、提问人、负责人可追溯；关闭不冒充知识更新 |
| 上线与恢复 | 草稿不泄漏；原资料不丢；恢复步骤可执行；渠道与解析实测单独记录 |

优先使用必要的接口／集成测试；真实渠道只补无法由后端证明的身份、引用和回复行为。不循环重测已通过场景，不用健康检查替代产品验收。

## 维护约定

需求变化改需求地图；顺序与依赖改本计划；代码／部署／验收证据改进度。旧记录只归档，不往当前入口不断追加矛盾快照。公共仓库不包含凭据、运行配置、客户资料或私有部署路径。

参考：[集成接口边界](OCTO-INTEGRATION.md)、[部署约定](OCTO-DEPLOYMENT.md)、[构建方法](OCTO-BUILD.md)。
