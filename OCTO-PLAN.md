# Octo × WeKnora 二次开发计划

更新：2026-09-14。状态：方案与仓库建立，业务集成未实现。

## 产品决定

以 WeKnora 为唯一知识管理后台，直接退出旧自建管理后台，不维护双后台或旧后台兼容 API。OpenClaw 作为 Agent 运行时，Octo 作为群、子区与私聊入口。继承 WeKnora 的 Vue / TDesign 前端、Go 后端与现有数据库迁移机制。

本仓库 fork 自 Tencent/WeKnora。上游 README、许可与原有功能保留。不要把上游能力误称为本项目新增成果。

## 目标场景

- 维护者在 WeKnora 管理知识、来源、使用范围和用户反馈。
- 群成员通过 Octo 小丘获取有来源的答案；信息不足时澄清，确认缺口后登记并告知联系人。
- 同一知识库服务多个群，一个群/子区可以绑定多个知识库。
- 群与子区的对话、反馈默认独立；知识和问题汇总分别配置，汇总只读不获得维护权限。
- 私人通用助理与公共知识助理保持身份、工作区、权限隔离。

不做：自动通知负责人、自动发布知识、强制报告格式、重写 Agent 运行时、重新实现 Loop 中继。本轮不引入 OpenViking。

## 架构及数据归属

```text
WeKnora 原生管理后台 + Octo 扩展模块
  ├─ 原生知识库 / 文档 / 解析 / 索引 / 权限
  ├─ 群与子区绑定 / Octo 身份映射
  ├─ 问题反馈及负责人信息
  └─ 目录来源与更新提案

Octo → 官方渠道插件 → OpenClaw → 受限知识工具
                                      ↓
                         WeKnora Octo 集成 API
                                      ↓
                      授权知识库及目录来源
```

优先直接使用 WeKnora KB ID。GitHub、本地源码、Markdown 是来源；RAG 是资料解析与检索能力。只有不依赖 WeKnora KB 的源码/MD-only 场景确需独立身份时，另行评估，不能先建第二套知识库实体。

现有部署的脱敏结构核对：3 个来源各自绑定不同 WeKnora KB；2 个 local_repo 和 2 个 local_markdown 没有显式 KB ID；未发现多来源显式指向同一 KB。目录与 KB 的组合关系需要显式关联，不能仅按名称推断。

新增 Octo 业务状态优先进入 WeKnora 数据库的独立业务表，遵循现有 tenant/workspace 约束。原文保留在既有资料存储与受限目录。OpenClaw 会话仍归运行时。旧 SQLite 是否导入有效数据由数据清单决定，不承诺保留测试记录，不为旧后台维持兼容服务。

## 权限约束

1. 用户、Bot、群、子区、消息 ID 来自可信入站；不能接受模型自报身份。
2. 查询授权、资产维护授权、默认草稿目标是三个不同概念。
3. 群管理员不是平台管理员；绑定不改变资产所有权。
4. 跨群共享必须有资产授权，接收群管理者再决定是否绑定。
5. 选择默认目标不会赋予写权；目标不明时由用户选择，AI 不猜。
6. 普通 Bot 默认不响应；任何例外由可验证授权控制。
7. 服务凭据不得变成模型可任意使用的全库访问令牌。集成 API 必须再次校验范围。

## 功能复用与改动范围

| 能力 | 策略 |
|---|---|
| 知识库与资料 CRUD、解析、索引 | 复用原生实现 |
| 现有身份、工作区、共享、API key | 先核对能否映射 Octo，避免重复 RBAC |
| 群/子区列表、绑定 | 新增 Octo 管理页与集成 API |
| 知识库使用范围 | 扩展原生知识库详情 |
| 提问人、登记人、负责人、反馈 | 新增反馈模块；保留原始消息关联 |
| 源码与 Markdown | 来源关联、授权目录读取、提交与行号引用 |
| 同步与提案 | 复用现有能力，缺口才扩展；仓库拉取与索引发布分开 |
| OpenClaw 侧 | 薄插件传递可信上下文、调用工具、组织回执 |

改名/备注一次保存并审计。删除、跨范围共享、正式应用更新展示影响后确认。不把每个写操作都建成多步审批流程。

## 执行里程碑

| 阶段 | 交付 | 完成标准 |
|---|---|---|
| P0 基线与退出 | 纳入现有 WeKnora 补丁；核对当前部署与 fork 差异；停用并归档旧后台、移除专用入口 | 旧后台不再提供服务；OpenClaw/Octo/WeKnora 保持可用；不误删共用插件/资料 |
| P1 身份与绑定 | 原生 KB 详情增加使用范围；群/子区管理；受限查询接口 | 一个群真实问答成功，另一个群不能越权读取；写权限与查询分离 |
| P2 反馈闭环 | 缺口/Bug 登记、澄清、责任信息、查询汇总 | 原生提问人和来源可追溯；跨区隔离；不自动通知或发布 |
| P3 来源与更新 | 目录/仓库关联、固定版本引用、差异提案 | 回答可跳转实际代码位置；源码已同步不误报索引已更新 |
| P4 收口 | 部署、配置指南、备份恢复、必要回归 | 唯一后台入口清晰；无双份业务真相；异常不冒充成功 |

## 已知风险与核对项

- 当前服务器 WeKnora 已有草稿可见性补丁，不能直接被上游覆盖。先做差异审查并纳入提交。
- fork 上游版本与运行版本可能不同，源码存在功能不代表部署已启用。
- WeKnora 原生 workspace/RBAC 与 Octo 群管理的映射尚未完成源码级评估。
- 源码目录读取不会因放入 WeKnora 就自然获得安全边界，需限制路径、符号链接和版本。
- 直接退出旧后台是产品决定，不等于删除整个旧插件目录；依赖核查后按清单清理。

## 开发与发布约定

- origin 为个人 fork；upstream 为 Tencent/WeKnora。
- main 保存可复核提交；功能分支通过 PR 合并，记录验证和未覆盖项。
- 上游按固定版本主动同步，不自动部署最新 main。
- 不提交凭据、运行配置、客户资料、聊天记录、数据库和私有服务器路径。
- 每阶段更新本计划状态。语法/单测、真实接口、Octo 渠道验收分别记录。

## 当前进度

- [x] 建立个人 fork 和二开计划。
- [x] 当前部署补丁纳入版本控制，PR #1 已合并；专项回归、Go lint 及 App 检查通过。
- [x] 旧管理后台实际停用；专用转发已移除，现有知识服务健康检查正常。
- [ ] WeKnora Octo 模块实现。
- [ ] 真实群闭环和隔离验收。

2026-09-14：现有草稿可见性补丁已提取并移入功能分支，兼容性测试与 PR 状态单独记录；未部署新 fork。旧后台保留一次归档，停止服务与自启动，不再继续开发旧页面。

用户已确认后续服务器 WeKnora 替换为本仓库构建版本。发布检查与保留数据边界见 [OCTO-DEPLOYMENT.md](OCTO-DEPLOYMENT.md)，不部署第二套自建后台。

部署进度：源码 `3b1c23053eebb3dbeb118607a44e54da6013bf69` 的前后端已正式替换。CI、隔离启动、数据库迁移、鉴权资料访问和一次真实混合检索已验证。构建网络优化与验证边界见 [OCTO-BUILD.md](OCTO-BUILD.md)。Octo 新管理模块尚未实现。
