# 实施计划与接续入口

更新：2026-09-23。需求以 [需求地图](OCTO-REQUIREMENTS.md) 为准，完成证据仅维护在 [进度](OCTO-STATUS.md)。

## 接手后的执行顺序

以下是下一轮工作的顺序和完成门槛，不表示本次文档接手已经执行这些操作。

1. **先核对运行中的资料处理。** 重新读取 `pending/processing/finalizing` 与运行中同步；9 月 23 日 10:13 CST 曾有 1 条 URL 资料处于 `finalizing`。先确定其完成或失败，再安排重启、恢复或解析回归。
2. **验收线上回答质量。** 用既有获授权的联调身份在「Steward 联调群」提出正式版本和三个 channel 仓库两类问题，逐条保存实际回答、来源 URL、工具调用数和耗时。只有真实消息正确投递、引文可打开且结论受证据支持，才能关闭回答质量回归；服务健康、单元测试和旧截图不替代它。
3. **逐条处理来源异常。** 针对 `octo-android`、`octo-server`、`octo-web` 三条文档入库来源，在当前版本单独诊断与有限重试。核对最后成功版本、失败类型与解析结果；不批量重跑 60 条，也不因重试失败清除旧知识。
4. **恢复单条中断候选。** 管理员对 `DAP_EVENTS.md` 所属 GitHub 文档来源先预览，再在候选文件可读且范围准确时显式确认；记录恢复 run 和最终知识状态。URL 来源另走原生重试或诊断，不混用 GitHub 文档恢复入口。
5. **接入真实 AIBP 内容。** 目前 AIBP 库为空；先确定获授权的原始资料和维护者，再用现有 KB 数据源、解析、草稿／发布流程接入。知识记录可检索、群绑定与越权拒绝分别验收。

若某一步暴露实现缺陷，再对该缺陷做局部修复和相应回归；不先扩展 OpenViking、Loop 或第二套管理层。任何新的发布继续遵守下文的备份、隔离候选和真实业务验收门槛。

### Agent 能力改进顺序（2026-09-23）

1. **P0 防错答覆盖**：证据策略只根据原始本轮问题分类，`GROUP.md`、引用、附件只进入模型背景；普通问题也要有明确的已分类标记。用“先问最新情况，再问 Octo 与 Loop 的关系”的连续对话回归，要求关系答复成为唯一最终答复，无无关发布兜底和额外重试。验证现有真实发布问题仍要求对应证据。
2. **P1 证据计划**：把单一关键词分类改为可并存的“概念关系、实现机制、当前发布”等信息需求。Agent 在获授权来源中选择查询顺序；服务端继续约束知识库、仓库、快照、预算和发送。逐步移除只针对少数固定仓库的预检特例。
3. **P2 主张与来源关联**：按目标实体、仓库、文档／代码位置、版本和检索完整性保存证据；“读过任意一份资料”不能替整段答案背书。对无权、未同步、超时、完整零命中、资料冲突分别说明，知识缺口不由工具故障触发。
4. **P3 知识与运营**：为同一仓库的文档入库和只读代码建立清晰的来源目录、新鲜度及健康视图；选择性维护可审核的产品 Markdown/Wiki，原始 GitHub/本地来源仍可追溯。Bug 登记、GitHub Issue、Loop Task 是不同动作；后两者只在明确授权、确认、幂等和审计具备后开放。管理员可查看脱敏的选源与兜底原因。

每一步用同一批真实场景比对答案正确性、引用、追问、越权拒绝、延迟和 token 消耗；代码测试与真实 Octo 群收发分别验收。OpenClaw 可继续承担私人或后台维护任务，不因公开知识问答体验问题而直接开放公共群的通用 Shell、MCP 或长期记忆。

| 工作包 | 交付 | 完成门槛 |
|---|---|---|
| P1 独立接管 | 原生 Octo 渠道、单一接收端、Agent、本人私聊、测试群绑定 | 真实群问答正确引用；旧接入仅移除公共 Bot；其他账号逐项不变 |
| P2 配置体验 | Token 身份核验、真实群／子区选择、双向绑定、私聊多库、资料空间发现／目录授权／安全选择 | 从页面完成新库接入；日常配置无需 JSON；工作区切换不串数据 |
| P3 知识业务 | 澄清登记、联系人、草稿／发布、管理确认、查询／报告／文件及周报 | 当前真实身份与权限每次复核；草稿不泄漏；同用户确认；真实发送结果 |
| P4 可靠性 | 持久消息记录、有限重试、原始资料保护、同步撤权边界、通用删除清理、同步失败保留旧版本 | 隔离、重启、重复消息、失败回读和删除重试验证 |
| P5 收口 | 真实部署、界面检查、文档与过时入口清理 | 运行版本可追溯；未验证项明确列出；主任务一次性交接 |

执行顺序：先形成独立可用闭环，再并行补配置与业务；不等待旧功能照搬完成才接管。只允许一个公共 Bot 接收端。新接入失败时修复本系统，不静默回退旧链路。

GitHub 批量接入和源码 Git 缓存已完成隔离候选验收并正式切换。今后的发布门槛是：从已有线上状态做一致性备份，在独立恢复库中完成来源同步验收，只重建受影响的应用／前端容器，随后回读健康、镜像、迁移、既有资产数量和 OpenClaw／Octo 运行状态。编译通过或页面健康都不足以代替这条链路。

来源删除采用“预览 → 显式模式 → 受限执行”契约：默认只解除来源，危险模式只清理元数据中精确归属的生成内容，并受短时确认令牌、当前操作者、候选集和资源引用计数共同约束。历史软删除来源不自动回扫；先形成审计预览，再由管理员确认具体对象。这个边界优先于节省存储空间。

长期存储边界：资料输入、源码快照、Git 缓存、临时处理、构建缓存和发布证据放入持久数据卷；容器运行目录只在文件系统兼容 overlayfs 时迁移。当前数据盘 XFS 不具备 `d_type`，所以系统 `containerd` 保持根盘，禁止用强制迁移或格式化当前数据盘来“解决”空间问题。系统日志以容量预算控制，避免历史日志重新占满根分区。

验收分三层记录：代码与自动测试、隔离数据库候选启动、真实 UI／Octo 行为。编译通过或健康检查不等于全部业务验收。

### 回答与来源质量的发布门槛

版本问题必须以官方 Release 为证据；点名仓库的问题必须逐仓读取固定版本源码；普通资料问答才使用 RAG/Wiki。预检、来源引用、查找完整性和重复读取限额由服务端执行。群内真实回归仍独立于单元测试和容器健康：上线后用既有联调身份验证一次版本问题和一次多仓问题，检查回复、链接、无关内容和读取次数。

### 发布前排空规则

重启 app 前同时核对：运行中的同步为 0，`pending/processing/finalizing` 知识为 0。已绑定 terminal knowledge 的历史 span 需要单独记录和清理，不能把它误判为正在执行的解析，也不能作为跳过前两项检查的理由。发布脚本保存 Compose、私有环境快照和数据库一致性导出；受影响容器启动失败时回到上一对镜像。

本轮不增加 OpenViking、Loop 中继、源码向量化、LSP、第二套后台或通用 Agent 平台。Git webhook、目录实时监听、源码语义符号索引按真实需求后续增加。
