# 新建一个 Octo 知识助理

本指南针对独立运行的 octo-weknora。无需安装或配置其他 Agent 运行时。

本文说明配置方法，不代替生产发布记录。当前镜像、真实消息验收和仍未验证的项目统一见 [OCTO-STATUS](OCTO-STATUS.md)。

## 最短配置顺序

1. 如需使用服务器资料目录，先由平台管理员进入“知识库 → 资料空间”：发现已挂载资料空间，登记空间，再把需要的相对目录授权给当前工作区。这个过程不上传文件、不触发同步，也不开放任意服务器路径。
2. 在当前工作区的模型与知识库页面配置模型，创建文档知识库。需要正式上传资料时由维护者按原生流程上传；需要持续同步时添加 GitHub 或服务器文件夹数据源。服务器文件夹从已授权目录树选择：用途选“源码与文本只读”时不向量化，选“文档入库”时才进入解析和 RAG 索引。同步成功后检查目录、状态和版本。
3. 在 Agent 页面创建智能推理 Agent，选择对话模型，启用知识检索、浏览只读来源和 Octo 知识业务工具。模型由 WeKnora 配置；解析、向量和对话模型各司其职。
4. 在“渠道接入 → Bot 与私聊 → 添加渠道”中选择 Agent 和 Octo。连接步骤可“新增或更新 Bot 连接”：填写 Token，先核验真实 Bot 名称与 UID，再保存连接；无需手填 Bot UID。新渠道默认停用，同一 Bot 的其他接收端停止后再启用。
5. 私聊默认不开放。需要时选择允许的原生用户 UID 和对应知识库；“仅允许创建者”只是填写快捷操作，保存后仍显示许可范围。
6. 在“渠道接入 → Octo 群与子区”页面选择连接，从 Octo 获取真实群，再选择子区。群由 Octo 客户端创建并邀请 Bot。把知识库绑定给目标区域；也能从知识库的“使用范围”反向绑定。
7. 普通绑定只允许查询。要群管理员维护，单独开启该库维护授权。需要群内建库时再开允许建库；已有模板库提供模型与解析默认值。
8. 在知识库联系人入口设置默认负责人及少量主题联系人。原生 UID 可用于可点击名片；不自动通知联系人。
9. 启用渠道，在目标群问一个有依据的问题，再问一个资料不足的问题。检查回复来源及问题登记记录。周报按需要单独选择来源区域、周期和接收位置后启用。

## 群、子区和权限

- 一个区域可绑定多个库，一个库可供多个区域查询。修改名称不改变 ID 和绑定。
- 子区“继承主群知识”只增加查询范围；维护许可需要独立授权。
- 主群“汇总子区问题”只增加问题查询范围，不赋予子区写权限；默认关闭。
- 解绑只取消读取关系。保留的维护授权在“可维护知识库”中可查看、重新绑定或撤销。
- 普通 Bot 默认忽略。测试 Bot 先加入查询白名单；确需维护时再加入维护白名单，只能操作该区域已授权的库。原生管理员信息缺失时系统不会凭名字猜测。
- 私聊是独立授权入口，不会自动得到所有群的知识或消息。

## 文件夹不是单个文件

服务器文件夹保存原来的目录层级和相对路径。可指定章节、源码子目录或文档入口，数据源保存获准目录 ID 与相对目录。浏览器中的 Windows 路径不等于 Linux 服务可读路径。

平台管理员在“知识库 → 资料空间”完成“已挂载空间 → 当前工作区授权目录”；知识库管理员随后在“知识库 → 设置 → 数据源”选择已授权目录。两个页面的职责刻意分开：前者管理服务器资料边界，后者决定一个知识库怎样使用资料。普通工作区管理员不能借页面读取宿主机任意绝对路径。

增加物理磁盘或全新挂载由运维处理；已有空间内新增资料目录可从目录树登记，无需改业务 JSON 或重启。完整说明见 [来源配置指南](OCTO-SOURCES.md)。

## 常见操作

| 目标 | 入口／自然语言示例 | 结果 |
|---|---|---|
| 查知识 | “根据当前知识库，如何安装 CLI？给出来源” | 引用实际资料；资料不足明确说明 |
| 反馈 Bug | “导出 CSV 一直转圈，预期下载文件” | 信息不足先澄清，再记录真实用户和区域 |
| 存资料 | “把这段说明保存为知识草稿，先预览” | 预览后由本人确认，尚未发布 |
| 发布资料 | “发布这条已核对的草稿，先给出预览” | 独立确认与权限检查，不把关闭问题当发布 |
| 查询／关闭问题 | “列出本区待处理问题”“关闭这条测试问题” | 只操作当前获准范围，保留处理记录 |
| 生成报告 | “最近七天的问题整理为 HTML，给我文件” | 查询真实登记记录并发送文件，不自动发布网页 |
| 排查失败 | 渠道接入 → Bot 详情 → 消息处理记录 | 查看处理／发送状态及失败原因，不暴露消息内容或凭据 |

维护确认使用 Bot 返回的原始操作编号，不能从别人的会话复制确认。资料在预览后被修改、权限撤销或确认过期时，需要重新预览。

## 上传、草稿与原稿位置

群里发送附件不会自动把附件发布到知识库。群内“保存知识草稿”是受限的文字草稿操作，保存后仍需单独确认发布。

网页上传文件、外部数据源同步与文字草稿不是同一条流程：原生文件在成功解析和索引后可能进入可检索状态，不能把“上传完成”统一解释成“待审核草稿”。需要审核的内容应先放在不对外绑定的知识库，或使用已支持的文字草稿流程，并检查最终可用状态。

WeKnora 手工维护的知识以原生文档为原稿；Git／服务器文件夹同步资料以源端为原稿。不要同时在页面和源目录手改同一份同步正文；资料来源、同步用途与原稿归属见 [来源配置指南](OCTO-SOURCES.md)。

## 周报与失败记录

在“问题反馈 → 报告与周报”中配置来源区域、发送渠道、星期和时间、格式及接收位置。默认不启用自动发送。私聊接收人仅能从该渠道已允许的 UID 中选择，并须具备来源区域访问权；发送前会重新核验。

“预览汇总”只读取真实登记数据，不会发送消息；CSV 下载与 Bot 发送附件是两个动作。Bot 文件发送使用 Octo 官方预签名上传流程，不能用已退役的 multipart 接口排错或要求模型自行上传任意本机文件。

IM 渠道的“消息处理记录”显示最近处理与投递状态。处理完成、未要求回复、权限变更以及投递失败分别处理；失败后先查原因，避免反复生成同一业务操作。

## 长期维护

GitHub 手动／定时同步后显示最后成功时间和固定提交；失败保留旧版本。源码默认不向量化，也不自动执行。已同步入库的副本与输入目录是不同生命周期，禁用来源不会悄悄删除已有正式资料。

周报默认关闭。报表只统计已登记问题，不代表全部对话量或真实问题总量。失败或发送不确定时先检查记录，再人工决定是否重发。卡片并非配置前提，普通回复、预览和确认可用文本完成。
