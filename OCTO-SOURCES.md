# 知识库来源配置与源码读取

更新：2026-09-21。本文描述已实现功能；部署与验收见 [进度](OCTO-STATUS.md)。

## 来源与用途

在“知识库 → 设置 → 数据源”中添加 GitHub 或服务器文件夹，再选择用途：

| 用途 | 行为 | 模型要求 |
|---|---|---|
| 文档入库 | 读取选定目录中的受支持文档，沿用原生解析和 KB 索引设置 | 开启 RAG 时使用嵌入模型；解析和摘要沿用 KB 配置 |
| 源码与文本只读 | 捕获目录快照，提供文件名／文本搜索和按行读取；不执行、不向量化 | 同步不调用模型；Agent 回答时使用它配置的对话模型 |

源码模式不按 JS/PHP/Python/Go 等语言后缀做白名单。UTF-8 文本源码、配置、脚本、无扩展名文件和文档都可读取。二进制、无法识别的编码、过大文件等会跳过并展示计数；这不等于执行解析所有二进制格式。

一个 KB 可以同时添加文档源和只读源。两份来源可指向同一仓库的不同范围，例如文档源选 `README.md` 和 `docs`，只读源选整个仓库。更换来源用途不自动删除此前已入库资料；需要保留持续更新的两种用途时，分别配置两份来源。

这里的 KB 是权限与知识资产单位，数据源是它的输入。群／子区通过 KB 建立多对多查询关系，不直接绑定磁盘路径。RAG 索引、源码快照与原始目录也不要求各建一套群目录或额外的业务 SQLite。

## GitHub

1. 公开仓库可以不填令牌；私有仓库使用只有 Contents 读取权限的令牌，沿用原生数据源凭据管理。
2. 填写 `owner/repository` 或 GitHub HTTPS 仓库地址。
3. 分支／ref 留空使用默认分支；可以指定分支、tag 或提交。若分支和 tag 同名，使用 `refs/heads/...` 或 `refs/tags/...`。
4. 每行一个文件或目录，留空表示整个仓库，再配置排除规则和同步频率。

公开和私有源码模式都通过服务器私有 Git 缓存解析固定提交：首次使用当前提交的浅克隆取得完整目录对象，后续同步复用已存在的 Git 对象与快照内容，不重复下载完整历史或每次重新取整仓归档。缓存按数据源隔离，令牌不写入 remote、快照、日志或浏览器响应；更新 GitHub 凭据或删除数据源会清除该数据源的生成传输缓存，已发布的只读快照按其生命周期保持可读。文档入库模式仍使用原有 GitHub 文档连接器流程，受对应 API 配额限制。

源码快照、Git 缓存和同步临时目录都是服务端私有持久化数据，部署时必须随应用数据卷放在持久数据盘。它们不会出现在浏览器、群消息、Agent 工作区或来源 API 中；只有经 KB 权限验证后的快照文件、行号和固定提交链接可以被读取或引用。

### 批量接入组织或作者

在“知识库 → 设置 → 数据源”选择“批量添加 GitHub 仓库”，输入组织或作者后先发现仓库，再明确勾选。发现本身不会创建数据源或启动同步；每个选中的仓库仍创建为一条普通 GitHub 数据源，因此它们各自拥有权限、同步状态、错误、日志和快照。

- 每页最多发现 100 个仓库，最多浏览 5 页；默认隐藏归档、停用和 fork 仓库。
- 每次最多创建 20 个仓库，超过时应分批创建。创建后默认不启动同步；可在结果确认后从各自的数据源卡片启动，或在批量页显式勾选“立即同步”。
- 同一知识库中已有相同“仓库＋用途”的来源会返回“已存在”，不会重复创建。源码只读和文档入库是两种独立用途；需要两者时分别创建。
- GitHub 同步在本进程默认最多并行 2 条，可由服务器管理员设置 `DATASOURCE_GITHUB_MAX_CONCURRENT`（1–8）。每个数据源的 Git 缓存默认最多 2 GiB，可用 `DATASOURCE_GITHUB_GIT_CACHE_MAX_BYTES` 在 64 MiB–8 GiB 间调整。

GitHub API 限流、认证失败、选中路径不存在、文档数量／容量超限、缓存传输失败和不完整文件会分别显示可行动的错误说明。失败不会发布半个源码快照，上一成功版本保持可读。

### 相同版本的同步复用

每次同步仍会先轻量核对远端 ref，避免把分支推进或 force-push 误判成“没有变化”。若同一数据源、同一选择范围和同一提交已存在完整快照，服务会验证该数据源私有对象的哈希、大小与行数后直接复用整份快照：不再 clone、fetch、遍历 Git 树或读取 blob。对象缺失或损坏时自动回到正常同步，不发布不完整快照。

这个复用范围只限同一数据源。不同知识库或不同数据源即使接入同一仓库，也各自保有快照、缓存和权限边界；它们不会共享私有代码对象或知识库内容。

默认排除依赖和构建目录，可按实际项目修改。已知密钥文件、版本库内部文件、符号链接、设备文件不读取。Git 子模块和 Git LFS 对象不会自动跨仓库／跨存储拉取，需要单独接入或先在获准本地目录准备好内容。

## 资料空间、目录授权与服务器文件夹

“服务器文件夹”指 WeKnora 服务进程已经能安全读取的资料目录，不是浏览器所在电脑的 `C:` 路径。它分成四个层次，页面不再把它们混在同一个弹窗里：

```text
宿主机资料目录
  └─ 只读挂载到容器 /source-roots/<空间名>
       └─ 资料空间（平台登记）
            └─ 已授权目录（某个工作区可用）
                 └─ 知识库数据源（选择用途、范围与同步）
```

- **资料空间**是服务部署已暴露的输入边界。`/source-roots` 是容器内统一入口，不是宿主机真实路径；宿主机到该入口的 bind mount 只保存在部署配置中。应用数据库只保存容器内空间路径，普通工作区页面不返回它。
- **自动发现**只扫描 `/source-roots` 的一级真实目录，跳过链接、隐藏版本目录和已登记空间。发现、登记都只读：不会挂载磁盘、创建目录、复制文件、授权工作区、创建数据源或触发同步。
- **已授权目录**是资料空间内的相对目录，单独授予一个工作区。平台管理员通过“知识库 → 资料空间”从目录树选择并命名；工作区管理员只能在数据源表单中选择已获授权的目录，不能猜 ID、输入 `/home`、`C:` 或相对穿越路径。
- **数据源**才决定知识库如何使用资料。一个授权目录可以被多个知识库使用；一个知识库可以使用多个授权目录、Git 仓库或上传资料。目录授权本身不会导入、解析或发布知识。

需要新增物理磁盘、NFS 或宿主机目录时，由服务器运维将它以只读方式暴露到 `/source-roots/<新空间>`，再由平台管理员在资料空间页执行“发现已有挂载 → 登记资料空间 → 授权目录”。这是受控运维动作，不是网页的“智能挂载”。

基础设施只需提供只读输入挂载与独立快照卷。可选初次空间登记示例（示意 ID）：

```json
{
  "DATASOURCE_LOCAL_SPACES": "[{\"id\":\"product-docs\",\"name\":\"产品资料空间\",\"path\":\"/source-roots/product-docs\"}]",
  "DATASOURCE_SNAPSHOT_DIR": "/source-snapshots"
}
```

- 原有 `DATASOURCE_LOCAL_ROOTS` 只导入一次，之后数据库为目录权限权威；修改旧变量不会重新授权或复活已撤销权限。
- `DATASOURCE_LOCAL_SPACES` 可用于初始部署空间；后续已经挂载到 `/source-roots` 的空间在页面发现、登记。浏览器不直接选择任意宿主路径。
- 新增、改名、停用工作区授权目录通过页面生效；资料空间只报告健康状态，不提供“停用整个挂载”的假开关。
- 输入目录只读挂载；`/source-snapshots` 使用独立私有持久化卷，运行用户可写。
- 文件读取基于受约束目录句柄并核对身份，拒绝路径穿越和符号链接替换。扫描中发现文件变化时保留此前快照，不冒充整个可变文件系统具有原子快照语义。
- 停用授权目录会停止后续同步批次和源码快照读取；已经入库的 RAG 文档不会自动下架，原始文件也不会删除。正在运行的同步在下一次受限批次检查处停止，已经完成的单项导入不回滚。
- 删除数据源或知识库清理的是生成缓存与平台资料，不会删除源文件夹或修改仓库。文档模式删除数据源不会自动删除已经入库的文档；源码快照模式会清理该来源的快照缓存。需要下架资料时使用对应的文档／知识库生命周期操作。

## 移除来源与生成内容

“移除来源”有两种明确结果，不能把它们混成一个无提示动作：

| 选择 | 会删除 | 会保留 |
|---|---|---|
| 仅移除来源 | 数据源配置、计划任务、私有 Git 缓存和源码快照 | 已入库的知识、向量和文档 |
| 移除来源并清理生成内容 | 上述内容，以及服务端能精确归属到该数据源的知识、分块、向量、图谱关联和最后一个引用的生成文件 | GitHub 远程仓库、服务器原始目录、其他来源或其他知识库的内容 |

管理者发起删除时，页面先读取影响预览，再给出两种选择；默认是“仅移除来源”。危险选项必须携带短时、签名、绑定当前操作者和预览候选集的确认凭据。若来源内容在预览后变化，服务会拒绝执行并要求重新预览。

数据源只属于一个知识库，归属通过 `tenant + knowledge base + datasource_id` 精确核验。同一仓库被两个知识库接入时会形成两条独立数据源；删除其中一条不会影响另一条。一个知识库被多个群或子区使用时，它们共享同一条来源内容，清理会影响该知识库的所有使用范围。

生成文件先通过资源引用计数处理：仍有其他 owner 的文件保留，最后一个 owner 消失时才删除物理字节。旧版没有资源归属证据的 raw 文件路径不会由危险选项自动清理；系统会阻止该次内容清理，要求走单独的审计迁移，避免误删共享旧文件。

## 浏览、引用与 Agent

知识库标题栏以及数据源操作菜单提供“浏览只读来源”。支持目录浏览、文件名／文本搜索、分段读取和来源跳转。搜索结果可能受数量／时间限制，界面会提示进一步缩小范围。

Agent 在“工具配置 → 源码与文本只读”中启用“浏览只读来源”（`source_browse`），并配置允许的知识库范围。纯源码 Agent 不需要嵌入／重排模型；若同时使用语义检索工具，再按该工具配置相关模型。快速问答模式只走原生 RAG，源码问答应使用启用了该工具的智能推理 Agent。

工具只接受服务端已授权的完整 KB 范围。只选择某篇文档或某个标签时，不会自动扩大为整个 KB 的源码范围。来源、文件路径和引用由服务端产生，模型不能自行选择账户、宿主机目录或别人的知识库。

- GitHub 文件返回固定提交与 `#L起始-L结束` 链接。
- 本地 Git 文件只有在远端提交可访问、文件内容与该提交相符时，才返回仓库链接；未提交改动、未推送提交或普通文件夹使用平台快照链接。
- 平台链接包含来源 ID、快照 ID、相对路径和行号，打开后仍需通过原生 KB 权限检查。
- 一次回答使用返回的快照 ID 固定证据。它是最近成功同步版本，不代表每次提问都即时联网拉取最新代码。
- 源码引用接入原生模型引用协议；不同代码行保留独立锚点，IM 层复用引用即可，无需再构建源码中继。

## 只读 API

以下路径均在 `/api/v1` 下，复用原生 Viewer／KB 读权限及 API Key 的 `retrieve` 范围；不会返回连接凭据或宿主机路径。

| 路径 | 用途 |
|---|---|
| GET `/knowledge-bases/:id/sources` | 列出可读来源、快照、状态、文件数和跳过统计 |
| GET `/knowledge-bases/:id/sources/:source_id/tree` | `snapshot_id`、`directory`、`offset`；每页最多 200 项 |
| GET `/knowledge-bases/:id/sources/:source_id/search` | `snapshot_id`、`q`、`path`；最多 40 个匹配，返回是否完整 |
| GET `/knowledge-bases/:id/sources/:source_id/read` | `snapshot_id`、`path`、`start_line`、`end_line`；返回正文、版本及引用 |

目录管理接口也在 `/api/v1` 下。`/system/admin/local-source-roots/spaces`、`/spaces/discovered`、`/spaces/:space_id/directories` 和授权目录 CRUD 只允许平台管理员；`/datasource/local-roots/:root_id/directories` 只允许当前工作区具有数据源管理权限的管理员，且仅返回相对目录。空间统计是所有工作区的基础设施统计，授权目录用量是当前工作区统计，界面分别标识。

服务接口是 `interfaces.SourceSnapshotReader`。非 HTTP 适配器必须携带为真实调用者解析的原生 KB read grant；执行租户 ID 本身不构成授权。

## 当前边界

- 源码快照最多 50000 个文本文件、单文件 4 MiB、总文本 256 MiB。GitHub 源码读取使用受限浅克隆缓存；缓存和快照都有独立容量边界，超限不会发布不完整版本。
- 单次读取最多 200 行、64 KiB，搜索为字面文本匹配。没有 LSP／AST 符号分析，也不承诺自动理解或执行整个仓库。
- 同步采用原生定时任务与手动触发，尚未提供 webhook 或文件系统实时监听。组织的新仓库不会自动纳入既有知识范围，必须重新发现并明确选择。
- 文档入库按文件完成安全替换，不能把多文件同步当成全库原子切换；发布状态、解析进度与源码快照版本分别检查。同步成功不代表 AI 已重新理解所有改动，更不代表服务器安装版本与仓库提交一致。
- 历史快照和内容对象目前随数据源保留；删除来源／KB 会清理缓存。自动按时间保留及配额回收仍待完善。
- 完整源码接入已支持 GitHub 与批准的本地目录；GitLab 的既有文档同步保留，尚未扩展为源码快照。
- 已软删除的历史数据源不会被新删除按钮自动回扫。管理员应先生成精确预览，再按审计范围清理其仍保留的生成知识；不能把历史残留与当前来源的删除混在一次无边界操作中。
