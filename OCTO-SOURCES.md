# 知识库来源配置与源码读取

更新：2026-09-20。本文描述已实现功能；部署与验收见 [进度](OCTO-STATUS.md)。

## 来源与用途

在“知识库 → 设置 → 数据源”中添加 GitHub 或服务器文件夹，再选择用途：

| 用途 | 行为 | 模型要求 |
|---|---|---|
| 文档入库 | 读取选定目录中的受支持文档，沿用原生解析和 KB 索引设置 | 开启 RAG 时使用嵌入模型；解析和摘要沿用 KB 配置 |
| 源码与文本只读 | 捕获目录快照，提供文件名／文本搜索和按行读取；不执行、不向量化 | 同步不调用模型；Agent 回答时使用它配置的对话模型 |

源码模式不按 JS/PHP/Python/Go 等语言后缀做白名单。UTF-8 文本源码、配置、脚本、无扩展名文件和文档都可读取。二进制、无法识别的编码、过大文件等会跳过并展示计数；这不等于执行解析所有二进制格式。

一个 KB 可以同时添加文档源和只读源。两份来源可指向同一仓库的不同范围，例如文档源选 `README.md` 和 `docs`，只读源选整个仓库。更换来源用途不自动删除此前已入库资料；需要保留持续更新的两种用途时，分别配置两份来源。

这里的 KB 是权限与知识资产单位，数据源是它的输入。群／子区通过 KB 建立多对多查询关系，不直接绑定磁盘路径。RAG 索引、源码快照与原始目录也不要求各建一套群目录或额外的业务 SQLite。

## GitHub

1. 公开仓库可以不填令牌；私有仓库使用只有 Contents 读取权限的令牌，沿用原生数据源凭据管理。
2. 填写 `owner/repository` 或 GitHub HTTPS 仓库地址。
3. 分支／ref 留空使用默认分支；可以指定分支、tag 或提交。若分支和 tag 同名，使用 `refs/heads/...` 或 `refs/tags/...`。
4. 每行一个文件或目录，留空表示整个仓库，再配置排除规则和同步频率。

公开源码模式通过只读 Git ref 广告解析提交，再获取固定提交归档，避免依赖共享出口的匿名 REST 配额。私有源码模式保留经过认证的 API 与归档流程，令牌不转发到跳转后的下载站点。文档入库模式仍使用原有文档连接器流程，受对应 API 配额限制。

默认排除依赖和构建目录，可按实际项目修改。已知密钥文件、版本库内部文件、符号链接、设备文件不读取。Git 子模块和 Git LFS 对象不会自动跨仓库／跨存储拉取，需要单独接入或先在获准本地目录准备好内容。

## 服务器文件夹

“服务器文件夹”指服务进程可访问的目录，不是浏览器所在电脑的 `C:` 路径。运维先只读挂载目录，并按 WeKnora 工作区登记允许的根目录。维护者在页面选择根目录，可再指定子目录；不允许在群消息或表单中任意读取宿主机绝对路径。

平台管理员通过“管理可用目录”登记已挂载空间内的子目录并分配工作区。日常使用不维护目录 JSON。

基础设施只需提供只读空间挂载与快照卷。可选初次空间登记示例（示意 ID）：

```json
{
  "DATASOURCE_LOCAL_SPACES": "[{\"id\":\"product-docs\",\"name\":\"产品资料空间\",\"path\":\"/source-roots/product-docs\"}]",
  "DATASOURCE_SNAPSHOT_DIR": "/source-snapshots"
}
```

- 原有 `DATASOURCE_LOCAL_ROOTS` 只导入一次，之后数据库为目录权限权威；修改旧变量不会重新授权。
- 新增、改名、禁用工作区子目录通过页面生效；浏览器不直接选择任意宿主路径。
- 输入目录只读挂载；`/source-snapshots` 使用独立私有持久化卷，运行用户可写。
- 文件读取基于受约束目录句柄并核对身份，拒绝路径穿越和符号链接替换。扫描中发现文件变化时保留此前快照，不冒充整个可变文件系统具有原子快照语义。
- 删除数据源或知识库清理的是生成缓存与平台资料，不会删除源文件夹或修改仓库。

## 浏览、引用与 Agent

知识库标题栏以及数据源操作菜单提供“浏览只读来源”。支持目录浏览、文件名／文本搜索、分段读取和来源跳转。搜索结果可能受数量／时间限制，界面会提示进一步缩小范围。

Agent 在“工具配置 → 源码与文本只读”中启用“浏览只读来源”（`source_browse`），并配置允许的知识库范围。纯源码 Agent 不需要嵌入／重排模型；若同时使用语义检索工具，再按该工具配置相关模型。快速问答模式只走原生 RAG，源码问答应使用启用了该工具的智能推理 Agent。

工具只接受服务端已授权的完整 KB 范围。只选择某篇文档或某个标签时，不会自动扩大为整个 KB 的源码范围。来源、文件路径和引用由服务端产生，模型不能自行选择账户、宿主机目录或别人的知识库。

- GitHub 文件返回固定提交与 `#L起始-L结束` 链接。
- 本地 Git 文件只有在远端提交可访问、文件内容与该提交相符时，才返回仓库链接；未提交改动、未推送提交或普通文件夹使用平台快照链接。
- 平台链接包含来源 ID、快照 ID、相对路径和行号，打开后仍需通过原生 KB 权限检查。
- 一次回答使用返回的快照 ID 固定证据。它是最近成功同步版本，不代表每次提问都即时联网拉取最新代码。
- 源码引用接入原生模型引用协议；不同代码行保留独立锚点，IM 层复用引用即可，无需再构建源码中继。

## 只读 API

以下路径均在 `/api/v1` 下，复用原生 Viewer／KB 读权限及 API Key 的 `retrieve` 范围；不会返回连接凭据或宿主机路径。

| 路径 | 用途 |
|---|---|
| GET `/knowledge-bases/:id/sources` | 列出可读来源、快照、状态、文件数和跳过统计 |
| GET `/knowledge-bases/:id/sources/:source_id/tree` | `snapshot_id`、`directory`、`offset`；每页最多 200 项 |
| GET `/knowledge-bases/:id/sources/:source_id/search` | `snapshot_id`、`q`、`path`；最多 40 个匹配，返回是否完整 |
| GET `/knowledge-bases/:id/sources/:source_id/read` | `snapshot_id`、`path`、`start_line`、`end_line`；返回正文、版本及引用 |

服务接口是 `interfaces.SourceSnapshotReader`。非 HTTP 适配器必须携带为真实调用者解析的原生 KB read grant；执行租户 ID 本身不构成授权。

## 当前边界

- 源码快照最多 50000 个文本文件、单文件 4 MiB、总文本 256 MiB。GitHub 归档另有压缩／解压限额；超限不会发布不完整版本。
- 单次读取最多 200 行、64 KiB，搜索为字面文本匹配。没有 LSP／AST 符号分析，也不承诺自动理解或执行整个仓库。
- 同步采用原生定时任务与手动触发，尚未提供 webhook 或文件系统实时监听。
- 文档入库按文件完成安全替换，不能把多文件同步当成全库原子切换；发布状态、解析进度与源码快照版本分别检查。同步成功不代表 AI 已重新理解所有改动，更不代表服务器安装版本与仓库提交一致。
- 历史快照和内容对象目前随数据源保留；删除来源／KB 会清理缓存。自动按时间保留及配额回收仍待完善。
- 完整源码接入已支持 GitHub 与批准的本地目录；GitLab 的既有文档同步保留，尚未扩展为源码快照。
