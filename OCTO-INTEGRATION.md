# Octo 原生知识助理架构

更新：2026-09-23。部署与验收状态见 [OCTO-STATUS](OCTO-STATUS.md)，执行顺序见 [OCTO-PLAN](OCTO-PLAN.md)，操作见 [配置指南](OCTO-SETUP.md)。下文的“目标演进”是设计契约，不等于已经上线。

## 一条业务链路

Octo 原生消息 → WeKnora IM 适配器 → 身份／区域授权 → WeKnora Agent → 原生知识服务与受限业务工具 → 原会话回复。

不需要 OpenClaw 或其他运行时参与。插件源码仅是协议参考，不是依赖。每轮只有一个对话 Agent；解析、向量化等后台模型任务各自按原生配置执行。

## 目标演进：一个知识入口，多种受控证据

公共 Octo 小丘继续由 WeKnora 原生渠道接收。每轮明确区分**原始用户问题**、原生引用、`GROUP.md`／`THREAD.md`、附件和历史摘要；后几项可供模型理解，不得重新决定本轮硬性证据类别或扩大授权。一个问题可同时需要概念说明、当前状态和代码实现，不再要求互斥地只选 `source/release/integration` 中一种。Agent 可以在已授权目录里规划查找顺序；服务端固定查询范围、凭据、快照、调用预算、引用和最终投递。

来源候选先由知识库范围与项目／产品目录缩小，再组合已发布文档 RAG、经审核的 Wiki、固定提交的代码文件，以及与所问渠道相符的发布资料。回答证据按“具体主张 → 实体／仓库 → 来源类型 → 版本与位置 → 查找是否完整”关联。模型读过任意文档不等于全段结论已获证；一次超时、无权或未同步也不等于知识不存在。可以回答已核实的部分并明确剩余边界，不用另一类问题的固定兜底覆盖正确答案。

一个 GitHub 仓库在逻辑上是一条连接：规范仓库、受限凭据、跟踪 ref、固定提交和可复用 manifest。在相同租户／凭据安全域中只获取必要 Git 对象，然后形成独立的**文档投影**（选定 README、说明目录等进入解析／RAG）和**代码投影**（完整允许文本的只读目录及行号引用）。现有两条数据源 ID、知识库索引、权限和删除行为应保持兼容；先复用传输与缓存，再改页面为“一仓库，两种用途”。跨知识库仅可在权限不混用的前提下共享物理对象，查询权和索引仍属各自知识库。图片、Office、PDF 等应有可预览的范围和解析策略；整仓图片不能默认为产品文档。

同步按固定提交列出文件和大小，先预览选取范围，再分批处理变更 blob。单文件有持久状态、有限重试和幂等恢复；文档旧版保持可用，完整清单核验后才推进完整游标，不能把部分失败当作源端删除。查询与管理页分别展示“仓库已获取”“文件已解析”“索引已可检索”、上次完整版本和失败原因。计划按仓库／工作区错峰和限并发，Webhook 只负责提示变更，仍需补偿扫描。GitHub App 可作为组织授权的长期凭据方式；追加多个 PAT 不替代传输和调度改造。

公开群默认只开放经审计的只读知识工具与当前授权的反馈草稿／确认工具。Skills 可教 Agent 如何查证、澄清和表达，但不能赋权；新增 GitHub Issue 或 Loop Task 属于外部写操作，须单独连接、目的地映射、原生身份授权、预览确认、幂等和审计。私人小丘／OpenClaw 可承担较长的资料研究或知识草稿任务，审核后再进入正式知识库；它不成为公共问答的必需中继。

## 数据与边界

| 对象 | 权威存储／职责 |
|---|---|
| Bot 凭据 | `octo_connections` 原生 AES-GCM 加密；浏览器只得到配置状态和已验证身份 |
| 渠道／Agent | 原生 `im_channels`、`custom_agents`，负责启停、私聊许可、模型与工具 |
| 群／子区 | `octo_scopes`：工作区、账号、原生群 ID、子区 ID、真实名称、继承／汇总策略 |
| 查询绑定 | `octo_scope_bindings` 引用原生 KB；同工作区多对多，解绑不删库 |
| 维护授权 | `octo_scope_knowledge_grants` 独立授权；解绑查询不撤销维护，页面可显式撤销 |
| 资料与来源 | 原生 KB／文档／数据源、只读源码快照；不再使用外部 JSON 目录索引 |
| 服务器目录 | `local_source_spaces` 是部署批准的物理空间；`local_source_roots` 是平台管理员分给工作区的可读子目录 |
| 问题／联系人／预览 | `octo_knowledge_issues`、事件、联系人、限时操作预览；保存原生来源与真实处理结果 |
| 消息／报告 | `octo_message_inbox` 持久收发状态；`octo_report_schedules`／runs 配合原生任务队列 |

群、子区、私聊的身份与会话范围独立；共享知识不共享对话，也不共享待确认操作。子区知识继承只继承查询权。主群汇总子区问题是另一开关，使用独立的问题读取范围，不扩大知识检索或子区写权限。

原生群主／管理员还须具有当前区域的知识维护授权。缺少 `bot_admin` 时不猜测管理员身份；需要测试 Bot 维护时，管理员显式配置维护 Bot 白名单，且该 Bot 必须已在普通 Bot 白名单中并由平台验证确为本群 Bot。白名单不提供全工作区权限。

## Agent 工具与操作

`knowledge_search`、`source_browse`、Wiki 等复用原生授权服务。“Octo 知识业务”开关一次启用配置查询、联系人、问题、维护预览／确认和报告专用工具。它们共用业务服务，模型不再面对混合几十个可选字段的通用入口；模型参数不能指定身份、租户或任意发送目标。正文、群说明、附件均不授予权限。

知识缺口必须有本轮成功检索的服务端证据；检索错误、未检索不能伪装成缺口。问题不清楚先追问。问题状态为待处理、处理中、已解决、已关闭，与文档草稿／发布相互独立。

维护先生成精确预览。确认绑定当前账号、会话、原生用户、操作编号、过期时间和资源版本；别人引用确认命令不会接管操作。群内建库复用当前获准知识库的模型／解析默认值；空白工作区先在原生页面建首个库。共享库的全局删除须在页面核对影响。

GROUP.md／THREAD.md 每轮从当前 Octo 区域读取并带来源及版本进入上下文，不映射到其他运行时工作目录。内置渠道 skills 仅说明当前工具用法，不开放 shell、凭据、通用沙箱或第二个 Agent。

## 消息与报告可靠性

受理后的消息先持久化，再交原生队列。已生成回复保留为待发送结果，发送重试不重跑模型和维护操作。业务登记和确认有独立幂等键。用户／区域会话隔离，默认群回复原生 @ 提问者并引用完整原消息。

普通对话使用文本。生成的报告只使用查询到的登记数据，支持 Markdown／CSV／HTML 的内存文件发送，不读取任意宿主机路径、不自动发布分享。周报另行显式启用，发送前复核渠道、区域绑定及接收人；发送不确定时留下失败状态，不无限重发。

附件发送使用 Octo 官方预签名 GET → 存储 PUT → 原生消息流程，准确传递文件字节数及签名请求头；存储请求不携带 Bot Token 或 Cookie，不跟随重定向。上传前后再次检查报告生成时的权限范围；普通回复附原消息引用，定时报告不伪造被回复消息。上传接口成功和真实群内文件可读是两个验收层次。

## 配置与运维

业务配置在现有 Vue／TDesign 页面完成。Token 不回显；配置 API 使用原生登录和角色门禁。服务器挂载与加密主密钥仍属基础设施。

旧 `DATASOURCE_LOCAL_ROOTS` 一次性导入数据库并记录标记，运行不双读。数据源选当前工作区获准目录；新增普通子目录登记无需重启。禁用目录阻止后续源码读取／同步，已入库 RAG 副本按原生知识生命周期管理。

移除来源、解绑、删资料、删库是不同操作。删除清理可重试；物理文件／索引成功处理后再退休数据库记录，保留恢复线索；绝不删除输入源目录、GitHub 仓库或其他 Bot 数据。
